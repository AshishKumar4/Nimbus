# Phase 3 Merge — Progress Log

> **Session:** Phase 3 merge orchestration
> **Started:** 2026-05-04
> **Operator:** autonomous build agent (user away ~1 year)
> **Goal:** Merge W7 (`w7-rpc-streams`) into main without prod deploy.
> **Constraint:** Wrangler OAuth dead → prod deploys still deferred.

## Pre-merge state

- `main` HEAD: `7a835ed audit: Phase 2 merged to main + master roadmap updated`
- W7 branch HEAD: `f4bb4e8 w7 F: retro + master roadmap status flip — branch-complete`
  - 9 commits past `7a835ed` (synced to origin)
  - Touches: `src/_shared/w7-frame.ts` (new, 412 LOC), `src/sqlite-vfs.ts` (+41), `src/supervisor-rpc.ts` (+26), `src/nimbus-session.ts` (+32), `src/npm-installer.ts` (+8), `src/npm-install-batch-facet.ts` (+40), `src/parallel/generated-workers.ts` (+18), `scripts/bundle-facet-workers.mjs` (~85 added), 18 audit files (probes + plan + retro + progress + roadmap edit).
- Pre-merge dirty file: `audit/probes/regression/install-pipeline-coverage.txt` was a probe artifact (timestamp/SID drift only — same noise W3-retro §S7 / phase2-merge-progress §54 documented). Stashed before merge to keep working tree clean.
- Pre-existing tsc errors on main (baseline, allowed):
  - `src/esbuild-service.ts(153,28)` — Cannot find module `esbuild-wasm/esbuild.wasm`
  - `src/nimbus-session.ts(2637,39)` — `SqliteVFSProvider` not assignable to `MountProvider`

## File overlap analysis (W7 vs main)

| File | W7 changes | Main edits since fork | Conflict risk |
|---|---|---|---|
| `src/sqlite-vfs.ts` | +41 (new `writeStream` method) | W5 LRU decoupling already in main | LOW — additive new method, no shared lines |
| `src/nimbus-session.ts` | +32 (new `_rpcWriteBatchStream`) | W5 + W9 + W8 all edited (already in main) | LOW — purely additive |
| `src/supervisor-rpc.ts` | +26 (new `writeBatchStream` RPC) | untouched since fork | NONE |
| `src/npm-installer.ts` | +8 (preamble import wiring) | W4/W6/W8 edits in main | LOW — small surgical add |
| `src/npm-install-batch-facet.ts` | +40 (typeof-guarded migration) | untouched since fork | NONE |
| `src/parallel/generated-workers.ts` | +18 (regenerated preambles) | regenerated by W8 in main | LOW — generated file |
| `scripts/bundle-facet-workers.mjs` | ~85 added | W8 noise only | LOW |

W7 was deliberately structured as **purely additive** (new RPC method, new wire codec, new VFS method) with **typeof-guarded migration** at the only call site. The retro §2 documents that backwards-compat was the safety mechanism.

## Merge — W7 (origin/w7-rpc-streams → main)

- Merge commit: `8b9ac44 Phase 3 merge: W7 (streams over RPC, bypass 32 MiB wall: frame codec, writeStream, writeBatchStream RPC, install-batch migration)`
- Conflicts: **none**. The additive design held — all 8 changed source files merged cleanly without overlap on any modified hunk. Even the regenerated `src/parallel/generated-workers.ts` (which W8 also regenerated in main) merged without manual intervention because W7's regen produced a strict superset of the W8 preamble (TAR_STREAM_PREAMBLE + W7_FRAME_PREAMBLE concatenated; W8's tar-only preamble is preserved verbatim).
- The W7 branch already included a roadmap edit ("branch-complete" → flipped to "Merged to main" post-merge).
- tsc state: **2 baseline errors only** ✅
  - `src/esbuild-service.ts(153,28)` — pre-existing W6 baseline
  - `src/nimbus-session.ts(2637,39)` — pre-existing W5/W9/W8 baseline (line still 2637 because W7 added its `_rpcWriteBatchStream` method below line 2637, not above)
- Local probes: **15/15 ALL W7 PROBES PASS** via `bun audit/probes/w7/run-all.mjs`:
  - functional 8/8 (frame-roundtrip, large-payload, backpressure, cancel-mid-stream, error-propagation, empty-batches, bytes-source-type, writestream-on-vfs)
  - regression 4/4 (install-pipeline-coverage, legacy-writeBatch-still-works, mossaic-shape, rpc-contracts-additive)
  - e2e 3/3 (synthetic-50mb-tarball, heap-peak-during-install — observed 0.23 MiB encoder peak, install-batch-facet-streams)
- The cross-suite regression check from W7-retro §5 was already exercised on the branch tip (w4 6/6, w5 7/7, w6 17/17, w8 21/21, w9 6/6). Re-running them post-merge would only update timestamp artifacts; skipped.
- The repo-level `audit/probes/regression/install-pipeline-coverage.mjs` requires a deployed BASE (`_driver.mjs` talks HTTP to a Workers URL). Not runnable locally, deferred to prod alongside the rest of the prod acceptance gates.
- Push: succeeded → `7a835ed..8b9ac44 main -> main`.

## Master roadmap update

- Phase 3 row: "✅ COMPLETE (code merged, prod deploy deferred)"
- W7 status cell: "✅ Merged to main 2026-05-04 — prod deploy DEFERRED (wrangler auth pending user OAuth). 15/15 local probes GREEN, tsc clean (only 2 baseline errors), no merge conflicts. See W7-retro.md. Heap-peak: 0.23 MiB observed vs 30 MiB target (16× over)."
- "Last updated" header → "Phase 1 + Phase 2 + Phase 3 merged to main"
- "Pending Prod Deploys" — added W7 row with the three acceptance probes (5GB monorepo install bypass, ≥30% latency reduction vs Mossaic baseline, supervisor-side peak heap 48→30 MiB). Facet-side heap reduction is already proved (0.23 MiB observed, 16× over the 30 MiB target).
- Batch deploy procedure step 5 — added Phase 3 block: `bun audit/probes/w7/run-all.mjs`
- Acceptance-failure step 7 — added W7.5 to the candidate follow-up wave list.
- Push grant note — bumped to mention Phase 3 push and `8b9ac44` as the new local main tip.

## Final state — Phase 3 complete

- main HEAD: `8b9ac44 Phase 3 merge: W7 ...` → followed by the roadmap+progress commit
- main is now Phase 1 + Phase 2 + Phase 3 complete (W3, W4, W5, W6, W7, W8, W9 all merged).
- tsc: 2 baseline errors only ✅
- Push grant: alive throughout this session.
- What's ready for prod deploy: everything in main. The single remaining gate is wrangler OAuth.
- What's pending prod measurement (now that code is in): W7 latency baseline (≥30% target) and W7 supervisor-side heap-peak (48→30 MiB target). Facet-side heap-peak already measured locally (0.23 MiB, 16× over target).
- Next phase to start: Phase 4 (W10 wrangler dev, W11 frameworks) — both pending. Per the roadmap, those are independent waves and can run parallel; gate is at planning, not at this merge.
