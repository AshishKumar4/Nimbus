W6.5 Phase D — Mossaic-interaction grep (per W6.5-plan §9 step 5)

Goal: confirm none of the 4 Mossaic scenarios (fastify, express, ts-jest+jest+typescript, redis)
transitively pull in any of the new REJECT entries added in W6.5.

New W6.5 REJECT entries:
  - @img/sharp-wasm32
  - @napi-rs/canvas
  - @napi-rs/canvas-wasm32-wasi

Method: build a real npm lockfile in /tmp/mossaic-check via
  npm install --package-lock-only fastify express ts-jest jest typescript redis

Then grep the lockfile for each REJECT name.

Result (2026-05-05):
  @img/sharp-wasm32              : 0 hits
  @napi-rs/canvas                : 0 hits
  @napi-rs/canvas-wasm32-wasi    : 0 hits

Conclusion: zero risk that the new REJECT entries break any Mossaic scenario.
The W6 regression meta-probe (audit/probes/w6/regression/install-pipeline-coverage-meta.mjs)
remains green. The full prod Mossaic probe (audit/probes/regression/
install-pipeline-coverage.mjs) is gated on prod auth and will run on the
next prod-deploy cycle (per W6 retro §7 pattern).
