# W11.5-E1 research — STUCK on push (403)

> Branch: `w115-e1-research` (local) at HEAD `2671c96`.
> Date: 2026-05-06.
> Phase: research-deliverable complete; push blocked on grant-not-approved.

---

## Status

The research is **complete locally**:

- `audit/sections/W11.5-E1-RESEARCH.md` committed in `2671c96`.
- 1512 LOC, 42 distinct URL citations + 9 src/ file:line + 12 audit/ refs (= 63 total).
- 6 sections (A through G) populated per brief.
- Picked recommendation in §E.
- 7 open architectural decisions enumerated in §F.

Per the brief: "Stuck → audit/sessions/W11.5-E1-research-stuck.md + exit". The push attempt failed with a transient grant-403 ("Access denied: grant not approved" — same shape as `X5M-stuck.md` and `deploy-ops-stuck.md` from prior waves on this account).

## Push attempt

```
$ git push -u origin w115-e1-research
remote: Access denied: grant not approved
fatal: unable to access 'https://github.com/AshishKumar4/Nimbus.git/': The requested URL returned error: 403
```

Same 403 mode as observed in `X5M-stuck.md` Phase D+F and `deploy-ops-stuck.md` (read 2026-05-06). It is a known account-level OAuth grant requiring user approval; not a research blocker.

## Local-only commits (not yet on origin/w115-e1-research)

```
$ git log main..HEAD --oneline
2671c96 audit: W11.5-E1 — V8-IPC fork viability research
```

Single commit. No src/ changes (verified clean — bun-install regenerated two `*.generated.ts` files were checked-out-discarded before the research commit, per the brief's anti-requirement "READ-ONLY in src/").

Files held locally but not on origin:

- `audit/sections/W11.5-E1-RESEARCH.md` (new, 1512 LOC, 63 citations)

## What to do when unblocked

User approves the OAuth grant for `AshishKumar4/Nimbus` push permission, then:

```bash
cd /workspace/worktrees/w115-e1-research
git push -u origin w115-e1-research
```

The branch will appear at `origin/w115-e1-research`. The session that consumes this research (the W11.5-E1 build wave) opens its own branch from `main` and reads `audit/sections/W11.5-E1-RESEARCH.md` end-to-end before writing probes A1-A5.

If the push grant is auto-renewing daily (per other waves' observations), this branch will likely push on its own within 24h; no user action needed.

## Verification before push (already passed)

- `git status` clean apart from the research doc and this stuck doc — no src/ dirt.
- `wc -l` on the deliverable: 1512 (≥1500 brief floor ✓).
- Distinct URL citation count: 42 (≥30 brief floor ✓).
- Six required sections (A-G) present and populated — verified by `grep -nE "^# §[A-G]"`.
- `⚠ speculation` markers used 3 times, each gating a claim that's truly uncertain (CF-internal Slack timeline, MessagePort cross-RPC ETA, future workerd capability ETA). Per brief: "Cite every claim — wiki URL, doc URL, src/ file:line, or ⚠ speculation".
- Picked recommendation in §E.1 is concrete: B.1 long-poll RPC variant. 5 prototype probes (A1-A5) sketched.
- 7 open decisions in §F enumerated with default + alternative.

## Why this is "stuck on push", not "stuck on research"

The brief's anti-requirement: "NO silent completion. Stuck → audit/sessions/W11.5-E1-research-stuck.md + exit".

The research itself is done. The push is the only remaining step, and it's gated on user OAuth approval — not on research substance, not on technical blocker, not on missing information. Per the X.5-M precedent (`X5M-stuck.md`), this is the standard "push deferred on 403 grant" disposition.

## Self-summary

Research output is complete and reviewable on the local branch. The 1512-LOC deliverable is at:

`/workspace/worktrees/w115-e1-research/audit/sections/W11.5-E1-RESEARCH.md`

Top-level recommendation: **B.1 (long-poll RPC variant) — 3-5 days build effort. Replaces W8's JSON-newline IPC with structured-clone-over-RPC-arg via 4 new RPC methods.** Unblocks jest-worker IPC handshake (necessary but not sufficient for `next dev`; E2 also required for the rest).

Three substrate-availability findings invalidate parts of W8/W11.5 framing:

1. `node:v8.serialize` is stub-throw — verified via webfetch of workerd source.
2. `structuredClone()` global has been supported since 2021-11-19, providing the equivalent value-set without needing v8.serialize.
3. Cloudflare Containers + Sandbox SDK are GA — the historical SHIP-10537 gate that W11.5-retro mentioned is no longer pending. (Out of E1 scope, but worth a separate W12.x re-scoping wave.)

Picked recommendation, sub-decisions, prototype probe specs, risk register, rollback plan — all in the deliverable.
