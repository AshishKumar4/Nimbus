/**
 * generated-workers.ts — AUTO-GENERATED. DO NOT EDIT.
 *
 * Produced by scripts/bundle-facet-workers.mjs from:
 *   - src/npm-tarball-stream.ts (streaming tar primitives)
 *   - src/_shared/w7-frame.ts   (W7 streaming bulk-write encoder)
 *
 * Consumed by src/parallel/facet-pool.ts callers via the `preamble`
 * option. The preamble is injected at the top of every generated
 * worker module so user functions can reference the exported
 * helpers by name.
 *
 * Tar-stream symbols: parseTarHeader, streamTarEntries,
 *   readableStreamToAsyncIterable, MAX_FILE_BYTES.
 * W7-frame symbols:   encodeWriteBatchStream, decodeWriteBatchStream,
 *   W7_MAGIC, W7_TRAILER.
 *
 * Generated at: 2026-05-04T23:05:33.977Z
 * Tar size: 3.84 KiB
 * W7 size:  7.77 KiB
 */

export const TAR_STREAM_PREAMBLE: string = "// src/npm-tarball-stream.ts\nvar MAX_FILE_BYTES = 2e7;\nfunction parseTarHeader(block) {\n  if (block[0] === 0) return null;\n  let name = \"\";\n  for (let i = 0; i < 100 && block[i] !== 0; i++) {\n    name += String.fromCharCode(block[i]);\n  }\n  let prefix = \"\";\n  for (let i = 345; i < 500 && block[i] !== 0; i++) {\n    prefix += String.fromCharCode(block[i]);\n  }\n  if (prefix) name = prefix + \"/\" + name;\n  name = name.replace(/^package\\//, \"\");\n  let sizeStr = \"\";\n  for (let i = 124; i < 136 && block[i] !== 0; i++) {\n    sizeStr += String.fromCharCode(block[i]);\n  }\n  const size = parseInt(sizeStr.trim(), 8) || 0;\n  const typeFlag = block[156];\n  return { name, size, typeFlag };\n}\nasync function* readableStreamToAsyncIterable(rs) {\n  const reader = rs.getReader();\n  try {\n    while (true) {\n      const { value, done } = await reader.read();\n      if (done) return;\n      if (value && value.length > 0) yield value;\n    }\n  } finally {\n    try {\n      reader.releaseLock();\n    } catch {\n    }\n  }\n}\nasync function* streamTarEntries(source, onSkip) {\n  let carry = new Uint8Array(0);\n  let state = { kind: \"header\" };\n  function concat(a, b) {\n    if (a.length === 0) return b;\n    if (b.length === 0) return a;\n    const out = new Uint8Array(a.length + b.length);\n    out.set(a, 0);\n    out.set(b, a.length);\n    return out;\n  }\n  for await (const chunkRaw of source) {\n    let buf = concat(carry, chunkRaw);\n    let cursor = 0;\n    while (true) {\n      if (state.kind === \"header\") {\n        if (buf.length - cursor < 512) break;\n        const header = buf.subarray(cursor, cursor + 512);\n        const parsed = parseTarHeader(header);\n        cursor += 512;\n        if (!parsed) return;\n        const { name, size, typeFlag } = parsed;\n        const pad = size === 0 ? 0 : (512 - size % 512) % 512;\n        const isRegularFile = typeFlag === 48 || typeFlag === 0;\n        if (size === 0) {\n          if (isRegularFile && name) {\n            yield { name, data: new Uint8Array(0) };\n          }\n          state = { kind: \"header\" };\n          continue;\n        }\n        if (!isRegularFile || !name || size > MAX_FILE_BYTES) {\n          if (onSkip) {\n            const reason = !isRegularFile ? \"non-regular\" : !name ? \"no-name\" : \"too-large\";\n            try {\n              onSkip(name, size, reason);\n            } catch {\n            }\n          }\n          state = { kind: \"skip\", remaining: size + pad };\n          continue;\n        }\n        state = {\n          kind: \"file\",\n          name,\n          remaining: size,\n          fileBuf: new Uint8Array(size),\n          fileOffset: 0,\n          pad,\n          skip: false\n        };\n        continue;\n      }\n      if (state.kind === \"file\") {\n        const avail2 = buf.length - cursor;\n        if (avail2 === 0) break;\n        if (state.remaining > 0) {\n          const take2 = Math.min(state.remaining, avail2);\n          state.fileBuf.set(buf.subarray(cursor, cursor + take2), state.fileOffset);\n          state.fileOffset += take2;\n          state.remaining -= take2;\n          cursor += take2;\n          if (state.remaining > 0) break;\n        }\n        if (state.pad > 0) {\n          const avail22 = buf.length - cursor;\n          if (avail22 === 0) break;\n          const take2 = Math.min(state.pad, avail22);\n          state.pad -= take2;\n          cursor += take2;\n          if (state.pad > 0) break;\n        }\n        yield { name: state.name, data: state.fileBuf };\n        state = { kind: \"header\" };\n        continue;\n      }\n      const avail = buf.length - cursor;\n      if (avail === 0) break;\n      const take = Math.min(state.remaining, avail);\n      cursor += take;\n      state.remaining -= take;\n      if (state.remaining > 0) break;\n      state = { kind: \"header\" };\n    }\n    if (cursor >= buf.length) {\n      carry = new Uint8Array(0);\n    } else if (cursor === 0) {\n      carry = buf;\n    } else {\n      carry = buf.slice(cursor);\n    }\n  }\n}";

export const TAR_STREAM_PREAMBLE_SIZE: number = 3932;

export const W7_FRAME_PREAMBLE: string = "// src/_shared/w7-frame.ts\nvar W7_MAGIC = new Uint8Array([78, 87, 55, 1]);\nvar W7_TRAILER = new Uint8Array([78, 69, 78, 68]);\nvar ENCODER_QUEUE_HWM = 256 * 1024;\nvar ENCODER_EMIT_CAP = 64 * 1024;\nvar _peakInFlight = 0;\nvar _currentInFlight = 0;\nfunction _peakInFlightBytes() {\n  return _peakInFlight;\n}\nfunction _resetPeakInFlightBytes() {\n  _peakInFlight = 0;\n  _currentInFlight = 0;\n}\nfunction _trackInFlightAdd(n) {\n  _currentInFlight += n;\n  if (_currentInFlight > _peakInFlight) _peakInFlight = _currentInFlight;\n}\nfunction _trackInFlightSub(n) {\n  _currentInFlight -= n;\n  if (_currentInFlight < 0) _currentInFlight = 0;\n}\nfunction encodeWriteBatchStream(payload) {\n  const inodes = payload.inodes ?? [];\n  const chunks = payload.chunks ?? [];\n  const deletePaths = payload.deletePaths;\n  let phase = 0;\n  let chunkIdx = 0;\n  const enc = new TextEncoder();\n  const headerBytes = enc.encode(JSON.stringify({\n    inodes,\n    deletePaths,\n    chunkCount: chunks.length\n  }));\n  const source = {\n    type: \"bytes\",\n    pull(controller) {\n      try {\n        if (phase === 0) {\n          enqueue(controller, W7_MAGIC.slice());\n          phase = 1;\n          return;\n        }\n        if (phase === 1) {\n          const hdr = new Uint8Array(4 + headerBytes.length);\n          writeU32LE(hdr, 0, headerBytes.length);\n          hdr.set(headerBytes, 4);\n          enqueue(controller, hdr);\n          phase = 2;\n          return;\n        }\n        if (phase === 2) {\n          if (chunkIdx >= chunks.length) {\n            phase = 3;\n          } else {\n            const c = chunks[chunkIdx++];\n            const pathBytes = enc.encode(c.path);\n            const data = c.data instanceof Uint8Array ? c.data : new Uint8Array(c.data);\n            const headerSize = 4 + pathBytes.length + 4 + 4;\n            const headerOut = new Uint8Array(headerSize);\n            let o = 0;\n            writeU32LE(headerOut, o, pathBytes.length);\n            o += 4;\n            headerOut.set(pathBytes, o);\n            o += pathBytes.length;\n            writeU32LE(headerOut, o, c.chunkId);\n            o += 4;\n            writeU32LE(headerOut, o, data.length);\n            o += 4;\n            enqueue(controller, headerOut);\n            if (data.length === 0) return;\n            if (data.length <= ENCODER_EMIT_CAP) {\n              enqueue(controller, data);\n            } else {\n              for (let i = 0; i < data.length; i += ENCODER_EMIT_CAP) {\n                enqueue(controller, data.subarray(i, Math.min(i + ENCODER_EMIT_CAP, data.length)));\n              }\n            }\n            return;\n          }\n        }\n        if (phase === 3) {\n          enqueue(controller, W7_TRAILER.slice());\n          phase = 4;\n          controller.close();\n          return;\n        }\n        controller.close();\n      } catch (e) {\n        controller.error(e);\n      }\n    },\n    cancel() {\n      phase = 4;\n      chunkIdx = chunks.length;\n    }\n  };\n  return new ReadableStream(source, {\n    // Byte streams (`type: 'bytes'`) MUST use the default byte-counted\n    // strategy: highWaterMark is in bytes, size is implicit. Custom\n    // `size` callbacks are forbidden — they throw at construction.\n    highWaterMark: ENCODER_QUEUE_HWM\n  });\n  function enqueue(controller, bytes) {\n    _trackInFlightAdd(bytes.byteLength);\n    controller.enqueue(bytes);\n    queueMicrotask(() => _trackInFlightSub(bytes.byteLength));\n  }\n}\nasync function decodeWriteBatchStream(stream) {\n  const reader = stream.getReader();\n  const buf = new ByteBuffer(reader);\n  const magic = await buf.readExact(4, \"magic\");\n  if (!bytesEqual(magic, W7_MAGIC)) {\n    try {\n      reader.releaseLock();\n    } catch {\n    }\n    throw new Error(`w7-frame: bad magic, expected NW7\\\\x01, got ${Array.from(magic).map((b) => b.toString(16)).join(\" \")}`);\n  }\n  const hdrLenBytes = await buf.readExact(4, \"header-length\");\n  const hdrLen = readU32LE(hdrLenBytes, 0);\n  if (hdrLen < 0 || hdrLen > 64 * 1024 * 1024) {\n    throw new Error(`w7-frame: implausible header length ${hdrLen}`);\n  }\n  const hdrJson = await buf.readExact(hdrLen, \"header-json\");\n  let header;\n  try {\n    header = JSON.parse(new TextDecoder().decode(hdrJson));\n  } catch (e) {\n    throw new Error(`w7-frame: header JSON parse failed: ${e?.message || e}`);\n  }\n  if (!header || !Array.isArray(header.inodes) || typeof header.chunkCount !== \"number\") {\n    throw new Error(\"w7-frame: header missing required fields (inodes / chunkCount)\");\n  }\n  const chunkIter = {\n    [Symbol.asyncIterator]() {\n      let idx = 0;\n      let exhausted = false;\n      return {\n        async next() {\n          if (exhausted) return { value: void 0, done: true };\n          if (idx >= header.chunkCount) {\n            const trailer = await buf.readExact(4, \"trailer\");\n            if (!bytesEqual(trailer, W7_TRAILER)) {\n              throw new Error(\n                `w7-frame: bad trailer, expected NEND, got ${Array.from(trailer).map((b) => String.fromCharCode(b)).join(\"\")}`\n              );\n            }\n            try {\n              reader.releaseLock();\n            } catch {\n            }\n            exhausted = true;\n            return { value: void 0, done: true };\n          }\n          const pathLenBytes = await buf.readExact(4, \"chunk-path-length\");\n          const pathLen = readU32LE(pathLenBytes, 0);\n          if (pathLen < 0 || pathLen > 64 * 1024) {\n            throw new Error(`w7-frame: implausible path length ${pathLen} at chunk ${idx}`);\n          }\n          const pathBytes = await buf.readExact(pathLen, \"chunk-path\");\n          const path = new TextDecoder().decode(pathBytes);\n          const chunkIdBytes = await buf.readExact(4, \"chunk-id\");\n          const chunkId = readU32LE(chunkIdBytes, 0);\n          const dataLenBytes = await buf.readExact(4, \"chunk-data-length\");\n          const dataLen = readU32LE(dataLenBytes, 0);\n          if (dataLen < 0 || dataLen > 64 * 1024 * 1024) {\n            throw new Error(`w7-frame: implausible data length ${dataLen} at chunk ${idx}`);\n          }\n          const data = await buf.readExact(dataLen, \"chunk-data\");\n          idx++;\n          return { value: { path, chunkId, data }, done: false };\n        },\n        async return() {\n          exhausted = true;\n          try {\n            reader.releaseLock();\n          } catch {\n          }\n          return { value: void 0, done: true };\n        }\n      };\n    }\n  };\n  return {\n    inodes: header.inodes,\n    deletePaths: header.deletePaths,\n    chunkIter\n  };\n}\nfunction writeU32LE(out, off, n) {\n  out[off] = n & 255;\n  out[off + 1] = n >>> 8 & 255;\n  out[off + 2] = n >>> 16 & 255;\n  out[off + 3] = n >>> 24 & 255;\n}\nfunction readU32LE(buf, off) {\n  return (buf[off] | buf[off + 1] << 8 | buf[off + 2] << 16 | buf[off + 3] << 24) >>> 0;\n}\nfunction bytesEqual(a, b) {\n  if (a.length !== b.length) return false;\n  for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;\n  return true;\n}\nvar ByteBuffer = class {\n  constructor(reader) {\n    this.reader = reader;\n  }\n  chunks = [];\n  avail = 0;\n  done = false;\n  async readExact(n, label) {\n    if (n === 0) return new Uint8Array(0);\n    while (this.avail < n) {\n      if (this.done) {\n        throw new Error(\n          `w7-frame: stream ended ${this.avail} bytes into expected ${n}-byte ${label}`\n        );\n      }\n      const { value, done } = await this.reader.read();\n      if (done) {\n        this.done = true;\n      } else if (value && value.length > 0) {\n        this.chunks.push(value);\n        this.avail += value.length;\n      }\n    }\n    const out = new Uint8Array(n);\n    let copied = 0;\n    while (copied < n) {\n      const head = this.chunks[0];\n      const take = Math.min(head.length, n - copied);\n      out.set(head.subarray(0, take), copied);\n      copied += take;\n      if (take === head.length) {\n        this.chunks.shift();\n      } else {\n        this.chunks[0] = head.subarray(take);\n      }\n    }\n    this.avail -= n;\n    return out;\n  }\n};";

export const W7_FRAME_PREAMBLE_SIZE: number = 7954;
