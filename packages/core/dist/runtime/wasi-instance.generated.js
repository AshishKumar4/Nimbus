/**
 * wasi-instance.generated.ts — AUTO-GENERATED. DO NOT EDIT.
 *
 * Produced by scripts/bundle-facet-workers.mjs from:
 *   - src/runtime/wasi/preamble.ts
 *
 * The WASI snapshot_preview1 shim as a flat ESM body, for splicing into a
 * facet module source. wasi-instance.ts appends the wasi-threads scheduler
 * and re-exports the result as WASI_INSTANCE_PREAMBLE_SRC.
 *
 * Size: 70.31 KiB
 */
export const WASI_INSTANCE_BODY_SRC = "var WASI_TCP_PATH_PREFIX = \"/dev/tcp/\";\nvar WASI_LISTEN_PATH_PREFIX = \"/dev/nimbus/listen/\";\nvar WASI_ACCEPTED_PATH_PREFIX = \"/dev/nimbus/socket/\";\nvar socketPathPrefixes = [WASI_TCP_PATH_PREFIX, WASI_LISTEN_PATH_PREFIX, WASI_ACCEPTED_PATH_PREFIX];\nvar encoder = new TextEncoder(), decoder = new TextDecoder(\"utf-8\", { fatal: true });\nvar errno = {\n  EACCES: 2,\n  EAGAIN: 6,\n  EBADF: 8,\n  EBUSY: 10,\n  EEXIST: 20,\n  EFAULT: 21,\n  EFBIG: 22,\n  EINTR: 27,\n  EINVAL: 28,\n  EIO: 29,\n  EISDIR: 31,\n  ELOOP: 32,\n  EMFILE: 33,\n  ENAMETOOLONG: 37,\n  ENFILE: 41,\n  ENOENT: 44,\n  ENOMEM: 48,\n  ENOSPC: 51,\n  ENOSYS: 52,\n  ENOTDIR: 54,\n  ENOTEMPTY: 55,\n  ENOTSUP: 58,\n  EPERM: 63,\n  EPIPE: 64,\n  EROFS: 69,\n  ESPIPE: 70,\n  ESTALE: 72,\n  EXDEV: 75,\n  ENOTCAPABLE: 76\n};\nfunction filesystemErrno(error) {\n  if (error && typeof error === \"object\" && \"code\" in error && typeof error.code === \"string\")\n    return errno[error.code] ?? errno.EIO;\n  if (error instanceof RangeError)\n    return errno.EFAULT;\n  return errno.EIO;\n}\nfunction after(value, next) {\n  return value instanceof Promise ? value.then(next) : next(value);\n}\nfunction fail(code) {\n  throw Object.assign(new Error(code), { code });\n}\nvar ft = (type) => type === \"directory\" ? 3 : type === \"symlink\" ? 7 : 4;\nvar num = (value) => {\n  const n = Number(value);\n  if (!Number.isSafeInteger(n))\n    fail(\"EINVAL\");\n  return n;\n};\nfunction installAuthorityFilesystem(imports, options) {\n  const fds = options.fds;\n  const hostClose = imports.fd_close;\n  const memory = () => new Uint8Array(options.memory().buffer);\n  const view = () => new DataView(options.memory().buffer);\n  const u32 = (ptr, value) => view().setUint32(ptr, value, true);\n  const u64 = (ptr, value) => view().setBigUint64(ptr, BigInt(value), true);\n  const path = (ptr, length) => {\n    if (ptr < 0 || length < 0 || ptr + length > memory().length)\n      fail(\"EFAULT\");\n    const p = decoder.decode(memory().subarray(ptr, ptr + length));\n    if (p.includes(\"\\0\"))\n      fail(\"EINVAL\");\n    return p;\n  };\n  const fs = () => {\n    const authority = options.fs();\n    if (!authority)\n      fail(\"ENOSYS\");\n    if (options.synchronous) {\n      if (!authority.synchronous)\n        fail(\"ENOTSUP\");\n      return authority.synchronous;\n    }\n    return authority;\n  };\n  const entry = (fd) => {\n    const e = fds.get(fd);\n    if (!e || e.kind !== \"authority\" && e.kind !== \"resident\")\n      fail(\"EBADF\");\n    return e;\n  };\n  const handle = (fd) => {\n    const e = entry(fd);\n    if (e.kind !== \"authority\")\n      fail(\"ENOTCAPABLE\");\n    return e;\n  };\n  const resident =   new Map();\n  const residentBytes = options.residentBytes ?? 0;\n  const residentContent = (fs2, target, st) => {\n    const key = `${st.dev}:${st.ino}`;\n    const cached = resident.get(key);\n    if (cached && cached.revision === st.revision)\n      return cached.bytes;\n    return after(fs2.readFile(target), (bytes2) => {\n      if (bytes2 === null)\n        fail(\"ENOENT\");\n      resident.set(key, { revision: st.revision, bytes: bytes2 });\n      return bytes2;\n    });\n  };\n  const preopen = (fd) => {\n    const e = fds.get(fd);\n    return e?.kind === \"preopen\" ? e : null;\n  };\n  const socketPath = (fd, ptr, length) => {\n    let guest;\n    try {\n      guest = path(ptr, length);\n    } catch {\n      return false;\n    }\n    const base = fds.get(fd);\n    if (!guest.startsWith(\"/\") && base?.kind === \"preopen\") {\n      guest = base.wasiPath.endsWith(\"/\") ? base.wasiPath + guest : base.wasiPath + \"/\" + guest;\n    }\n    return socketPathPrefixes.some((prefix) => guest.startsWith(prefix));\n  };\n  const at = (fd, p) => {\n    if (p.startsWith(\"/\"))\n      fail(\"ENOTCAPABLE\");\n    const e = fds.get(fd);\n    if (e?.kind === \"preopen\")\n      return { root: e.vfsPath, path: underPreopen(e, p), beneath: true };\n    const opened = entry(fd);\n    if (opened.kind !== \"authority\" || opened.type !== \"directory\")\n      fail(\"ENOTDIR\");\n    return { directory: opened.handle.id, path: p, beneath: true };\n  };\n  const underPreopen = (e, p) => {\n    const root = e.vfsPath;\n    if (root.length === 0)\n      return p;\n    if (p === root)\n      return \"\";\n    return p.startsWith(root) && p.charCodeAt(root.length) === 47 ? p.slice(root.length + 1) : p;\n  };\n  const right = (fd, bit) => {\n    const e = entry(fd);\n    if (!(e.rights & 1n << BigInt(bit)))\n      fail(\"ENOTCAPABLE\");\n    return e;\n  };\n  const creationMode = (base) => options.umask ? base & ~options.umask() : void 0;\n  const pathRight = (fd, bit) => {\n    const e = preopen(fd) ?? entry(fd);\n    if (!((e.rights ?? 0x1fffffffn) & 1n << BigInt(bit)))\n      fail(\"ENOTCAPABLE\");\n    return e;\n  };\n  const stat = (fs2, fd) => {\n    const p = preopen(fd);\n    if (p)\n      return after(fs2.stat(p.vfsPath), (value) => value ?? fail(\"ENOENT\"));\n    const e = entry(fd);\n    return e.kind === \"resident\" ? e.stat : fs2.fstat(e.handle.id);\n  };\n  const writeStat = (ptr, st) => {\n    const dv = view();\n    const old = options.abi === \"preview0\";\n    memory().fill(0, ptr, ptr + (old ? 56 : 64));\n    u64(ptr, st.dev);\n    u64(ptr + 8, st.ino);\n    dv.setUint8(ptr + 16, ft(st.type));\n    if (old)\n      u32(ptr + 20, Number(st.nlink));\n    else\n      u64(ptr + 24, st.nlink);\n    const base = old ? 24 : 32;\n    u64(ptr + base, st.size);\n    u64(ptr + base + 8, BigInt(Math.trunc(st.atime)) * 1000000n);\n    u64(ptr + base + 16, BigInt(Math.trunc(st.mtime)) * 1000000n);\n    u64(ptr + base + 24, BigInt(Math.trunc(st.ctime)) * 1000000n);\n    return 0;\n  };\n  const guard = (previous, body, owns2) => (...args) => {\n    if (!options.fs() || owns2 && !owns2(args))\n      return previous ? previous(...args) : 52;\n    try {\n      const result = body(fs(), ...args);\n      if (result instanceof Promise) {\n        if (options.synchronous)\n          throw new Error(\"Filesystem synchronous contract returned a Promise\");\n        return result.catch(filesystemErrno);\n      }\n      return result;\n    } catch (error) {\n      return filesystemErrno(error);\n    }\n  };\n  const owns = (args) => {\n    const e = fds.get(Number(args[0]));\n    return e?.kind === \"authority\" || e?.kind === \"resident\" || e?.kind === \"preopen\";\n  };\n  const iovs = (ptr, count) => {\n    if (ptr < 0 || count < 0 || count > Math.floor((memory().length - ptr) / 8))\n      fail(\"EFAULT\");\n    const result = [];\n    let total = 0;\n    for (let i = 0; i < count; i++) {\n      const p = view().getUint32(ptr + i * 8, true), length = view().getUint32(ptr + i * 8 + 4, true);\n      if (p + length > memory().length)\n        fail(\"EFAULT\");\n      result.push({ ptr: p, length });\n      total += length;\n    }\n    return { result, total };\n  };\n  const scatter = (data, vectors) => {\n    let used = 0;\n    for (const v of vectors) {\n      const n = Math.min(v.length, data.length - used);\n      if (n <= 0)\n        break;\n      memory().set(data.subarray(used, used + n), v.ptr);\n      used += n;\n    }\n    return used;\n  };\n  const read = (fs2, fd, ptr, count, offset, written) => {\n    const target = fds.get(fd);\n    if (target?.kind === \"preopen\" || target?.kind === \"authority\" && target.type === \"directory\")\n      fail(\"EISDIR\");\n    const e = right(fd, 1), vectors = iovs(ptr, count);\n    if (e.kind === \"resident\") {\n      const start = Math.min(offset ?? e.position, e.bytes.length);\n      const used = scatter(e.bytes.subarray(start, Math.min(e.bytes.length, start + vectors.total)), vectors.result);\n      if (offset === null)\n        e.position = start + used;\n      u32(written, used);\n      return 0;\n    }\n    return after(fs2.read(e.handle.id, offset, vectors.total), (data) => {\n      u32(written, scatter(data, vectors.result));\n      return 0;\n    });\n  };\n  const write = (fs2, fd, ptr, count, offset, written) => {\n    const e = right(fd, 6), vectors = iovs(ptr, count), data = new Uint8Array(vectors.total);\n    let used = 0;\n    if (e.kind === \"resident\")\n      fail(\"ENOTCAPABLE\");\n    for (const v of vectors.result) {\n      data.set(memory().subarray(v.ptr, v.ptr + v.length), used);\n      used += v.length;\n    }\n    return after(fs2.write(e.handle.id, offset, data), (n) => {\n      u32(written, n);\n      return 0;\n    });\n  };\n  imports.path_open = guard(imports.path_open, (fs2, fd, lookup, p, n, flags, rights, inherit, status, out) => {\n    if (status & ~5)\n      fail(\"ENOTSUP\");\n    pathRight(fd, 13);\n    if (flags & 1)\n      pathRight(fd, 10);\n    if (flags & 8)\n      pathRight(fd, 19);\n    const target = at(fd, path(p, n));\n    const parent = preopen(fd) ?? entry(fd);\n    const requested = BigInt(rights), childRights = BigInt(inherit);\n    const allowed = parent.rightsInheriting;\n    if (allowed !== void 0 && (requested | childRights) & ~allowed)\n      fail(\"ENOTCAPABLE\");\n    const followSymlinks = !!(lookup & 1);\n    const readOnly = !(requested & (1n << 6n | 1n << 8n | 1n << 22n)) && !(flags & 15) && !(status & 1);\n    const open = () => after(fs2.open(target, {\n      read: !!(requested & 2n),\n      write: !!(requested & 64n),\n      append: !!(status & 1),\n      create: !!(flags & 1),\n      directory: !!(flags & 2),\n      exclusive: !!(flags & 4),\n      truncate: !!(flags & 8),\n      followSymlinks,\n      mode: creationMode(438)\n    }), (handle2) => after(fs2.fstat(handle2.id), (st) => {\n      const id = options.allocateFd();\n      fds.set(id, { kind: \"authority\", handle: handle2, type: st.type, rights: requested, rightsInheriting: childRights, fdflags: status });\n      u32(out, id);\n      return 0;\n    }));\n    if (!readOnly || residentBytes === 0)\n      return open();\n    return after(fs2.stat(target, { followSymlinks }), (st) => {\n      if (st === null)\n        fail(\"ENOENT\");\n      if (st.type !== \"file\" || st.size > residentBytes)\n        return open();\n      return after(residentContent(fs2, target, st), (bytes2) => {\n        const id = options.allocateFd();\n        fds.set(id, { kind: \"resident\", stat: st, bytes: bytes2, position: 0, rights: requested, rightsInheriting: childRights, fdflags: status });\n        u32(out, id);\n        return 0;\n      });\n    });\n  }, (args) => !socketPath(args[0], args[2], args[3]));\n  imports.path_filestat_get = guard(imports.path_filestat_get, (fs2, fd, flags, p, n, out) => {\n    pathRight(fd, 18);\n    return after(fs2.stat(at(fd, path(p, n)), { followSymlinks: !!(flags & 1) }), (st) => writeStat(out, st ?? fail(\"ENOENT\")));\n  });\n  imports.fd_filestat_get = guard(imports.fd_filestat_get, (fs2, fd, out) => {\n    pathRight(fd, 21);\n    return after(stat(fs2, fd), (st) => writeStat(out, st));\n  }, owns);\n  imports.fd_read = guard(imports.fd_read, (fs2, fd, p, n, out) => read(fs2, fd, p, n, null, out), owns);\n  imports.fd_pread = guard(imports.fd_pread, (fs2, fd, p, n, off, out) => read(fs2, fd, p, n, num(off), out), owns);\n  imports.fd_write = guard(imports.fd_write, (fs2, fd, p, n, out) => write(fs2, fd, p, n, null, out), owns);\n  imports.fd_pwrite = guard(imports.fd_pwrite, (fs2, fd, p, n, off, out) => write(fs2, fd, p, n, num(off), out), owns);\n  imports.fd_close = guard(imports.fd_close, (fs2, fd) => {\n    if (preopen(fd))\n      return 0;\n    const e = entry(fd);\n    if (e.kind === \"resident\") {\n      fds.delete(fd);\n      return 0;\n    }\n    return after(fs2.close(e.handle.id), () => {\n      fds.delete(fd);\n      return 0;\n    });\n  }, owns);\n  imports.fd_renumber = guard(imports.fd_renumber, (fs2, from, to) => {\n    const source = fds.get(from);\n    if (!source)\n      fail(\"EBADF\");\n    if (from === to)\n      return 0;\n    const target = fds.get(to);\n    if (target?.kind === \"preopen\")\n      fail(\"ENOTCAPABLE\");\n    const released = !target || target.kind === \"resident\" ? void 0 : target.kind === \"authority\" ? fs2.close(target.handle.id) : hostClose?.(to);\n    return after(released, () => {\n      fds.delete(from);\n      fds.set(to, source);\n      return 0;\n    });\n  }, (args) => owns(args) || owns([args[1]]));\n  imports.fd_seek = guard(imports.fd_seek, (fs2, fd, offset, whence, out) => {\n    const kind = options.abi === \"preview0\" ? whence === 2 ? \"set\" : whence === 0 ? \"current\" : whence === 1 ? \"end\" : null : whence === 0 ? \"set\" : whence === 1 ? \"current\" : whence === 2 ? \"end\" : null;\n    if (kind === null)\n      fail(\"EINVAL\");\n    const e = right(fd, 2);\n    if (e.kind === \"resident\") {\n      const base = kind === \"set\" ? 0 : kind === \"current\" ? e.position : e.bytes.length;\n      const pos = base + num(offset);\n      if (pos < 0)\n        fail(\"EINVAL\");\n      e.position = pos;\n      u64(out, pos);\n      return 0;\n    }\n    return after(fs2.seek(e.handle.id, num(offset), kind), (pos) => {\n      u64(out, pos);\n      return 0;\n    });\n  }, owns);\n  imports.fd_tell = guard(imports.fd_tell, (fs2, fd, out) => {\n    const e = right(fd, 5);\n    if (e.kind === \"resident\") {\n      u64(out, e.position);\n      return 0;\n    }\n    return after(fs2.seek(e.handle.id, 0, \"current\"), (pos) => {\n      u64(out, pos);\n      return 0;\n    });\n  }, owns);\n  imports.fd_fdstat_get = guard(imports.fd_fdstat_get, (fs2, fd, out) => {\n    const e = preopen(fd) ?? entry(fd);\n    memory().fill(0, out, out + 24);\n    view().setUint8(out, e.kind === \"preopen\" ? 3 : e.kind === \"resident\" ? 4 : ft(e.type));\n    view().setUint16(out + 2, e.kind === \"preopen\" ? 0 : e.fdflags, true);\n    u64(out + 8, e.rights ?? 0x1fffffffn);\n    u64(out + 16, e.rightsInheriting ?? 0x1fffffffn);\n    return 0;\n  }, owns);\n  imports.fd_fdstat_set_flags = guard(imports.fd_fdstat_set_flags, (fs2, fd, flags) => {\n    if (flags & ~5)\n      fail(\"ENOTSUP\");\n    const e = right(fd, 3);\n    if (e.kind === \"resident\") {\n      e.fdflags = flags;\n      return 0;\n    }\n    return after(fs2.setStatus(e.handle.id, { append: !!(flags & 1) }), () => {\n      e.fdflags = flags;\n      return 0;\n    });\n  }, owns);\n  imports.fd_fdstat_set_rights = guard(imports.fd_fdstat_set_rights, (fs2, fd, rights, inheriting) => {\n    const e = entry(fd);\n    if (rights & ~e.rights || inheriting & ~e.rightsInheriting)\n      fail(\"ENOTCAPABLE\");\n    e.rights = rights;\n    e.rightsInheriting = inheriting;\n    return 0;\n  }, owns);\n  imports.fd_filestat_set_size = guard(imports.fd_filestat_set_size, (fs2, fd, size) => {\n    right(fd, 22);\n    return after(fs2.ftruncate(handle(fd).handle.id, num(size)), () => 0);\n  }, owns);\n  imports.fd_sync = guard(imports.fd_sync, (fs2, fd) => {\n    const e = right(fd, 4);\n    return e.kind === \"resident\" ? 0 : after(fs2.fsync(e.handle.id), () => 0);\n  }, owns);\n  imports.fd_datasync = guard(imports.fd_datasync, (fs2, fd) => {\n    const e = right(fd, 0);\n    return e.kind === \"resident\" ? 0 : after(fs2.fsync(e.handle.id), () => 0);\n  }, owns);\n  imports.fd_allocate = guard(imports.fd_allocate, (fs2, fd, offset, len) => {\n    right(fd, 8);\n    const e = handle(fd), end = num(offset) + num(len);\n    return after(fs2.fstat(e.handle.id), (st) => st.size >= end ? 0 : after(fs2.ftruncate(e.handle.id, end), () => 0));\n  }, owns);\n  imports.fd_advise = guard(imports.fd_advise, (fs2, fd) => {\n    right(fd, 7);\n    return 0;\n  }, owns);\n  imports.fd_readdir = guard(imports.fd_readdir, (fs2, fd, buf, size, cookie, used) => {\n    pathRight(fd, 14);\n    const e = preopen(fd) ?? entry(fd);\n    if (e.kind === \"resident\")\n      fail(\"ENOTDIR\");\n    const list = e.kind === \"preopen\" ? fs2.readdir(e.vfsPath) : e.entries ?? fs2.readdirHandle(e.handle.id);\n    return after(list, (entries) => {\n      if (e.kind === \"authority\")\n        e.entries = entries;\n      const all = [{ name: \".\", type: \"directory\" }, { name: \"..\", type: \"directory\" }, ...entries];\n      let written = 0;\n      for (let i = num(cookie); i < all.length && written < size; i++) {\n        const name = encoder.encode(all[i].name), record = new Uint8Array(24 + name.length), dv = new DataView(record.buffer);\n        dv.setBigUint64(0, BigInt(i + 1), true);\n        dv.setUint32(16, name.length, true);\n        dv.setUint8(20, ft(all[i].type));\n        record.set(name, 24);\n        const n = Math.min(record.length, size - written);\n        memory().set(record.subarray(0, n), buf + written);\n        written += n;\n      }\n      u32(used, written);\n      return 0;\n    });\n  }, owns);\n  imports.path_create_directory = guard(imports.path_create_directory, (fs2, fd, p, n) => {\n    pathRight(fd, 9);\n    return after(fs2.mkdir(at(fd, path(p, n)), { recursive: false, mode: creationMode(511) }), () => 0);\n  });\n  imports.path_remove_directory = guard(imports.path_remove_directory, (fs2, fd, p, n) => {\n    pathRight(fd, 25);\n    return after(fs2.rmdir(at(fd, path(p, n))), () => 0);\n  });\n  imports.path_unlink_file = guard(imports.path_unlink_file, (fs2, fd, p, n) => {\n    pathRight(fd, 26);\n    return after(fs2.unlink(at(fd, path(p, n))), () => 0);\n  });\n  imports.path_rename = guard(imports.path_rename, (fs2, from, p, n, to, q, m) => {\n    pathRight(from, 16);\n    pathRight(to, 17);\n    return after(fs2.rename(at(from, path(p, n)), at(to, path(q, m))), () => 0);\n  });\n  imports.path_symlink = guard(imports.path_symlink, (fs2, p, n, fd, q, m) => {\n    pathRight(fd, 24);\n    return after(fs2.symlink(path(p, n), at(fd, path(q, m))), () => 0);\n  });\n  imports.path_readlink = guard(imports.path_readlink, (fs2, fd, p, n, buf, cap, used) => {\n    pathRight(fd, 15);\n    return after(fs2.readlink(at(fd, path(p, n))), (value) => {\n      if (value === null)\n        fail(\"EINVAL\");\n      const data = encoder.encode(value), count = Math.min(cap, data.length);\n      memory().set(data.subarray(0, count), buf);\n      u32(used, count);\n      return 0;\n    });\n  });\n  imports.path_link = guard(imports.path_link, (fs2, from, flags, p, n, to, q, m) => {\n    pathRight(from, 11);\n    pathRight(to, 12);\n    const source = at(from, path(p, n)), target = at(to, path(q, m));\n    return after(fs2.stat(source, { followSymlinks: !!(flags & 1) }), (st) => {\n      if (st === null)\n        fail(\"ENOENT\");\n      if (st.type === \"directory\")\n        fail(\"EPERM\");\n      return after(fs2.stat(target, { followSymlinks: false }), (existing) => {\n        if (existing !== null)\n          fail(\"EEXIST\");\n        return after(fs2.copyFile(source, target), () => 0);\n      });\n    });\n  });\n  const times = (st, a, m, flags) => {\n    if ((flags & 3) === 3 || (flags & 12) === 12 || flags & ~15)\n      fail(\"EINVAL\");\n    return [\n      flags & 2 ? Date.now() : flags & 1 ? Number(a / 1000000n) : st.atime,\n      flags & 8 ? Date.now() : flags & 4 ? Number(m / 1000000n) : st.mtime\n    ];\n  };\n  imports.fd_filestat_set_times = guard(imports.fd_filestat_set_times, (fs2, fd, a, m, flags) => {\n    right(fd, 23);\n    return after(stat(fs2, fd), (st) => after(fs2.futimes(handle(fd).handle.id, ...times(st, a, m, flags)), () => 0));\n  }, owns);\n  imports.path_filestat_set_times = guard(imports.path_filestat_set_times, (fs2, fd, lookup, p, n, a, m, flags) => {\n    pathRight(fd, 20);\n    const target = at(fd, path(p, n)), followSymlinks = !!(lookup & 1);\n    return after(fs2.stat(target, { followSymlinks }), (st) => after(fs2.utimes(target, ...times(st ?? fail(\"ENOENT\"), a, m, flags), { followSymlinks }), () => 0));\n  });\n}\n\nvar FILESYSTEM_RPC_METHODS = {\n  stat: \"stat\",\n  readFile: \"readFileBytes\",\n  writeFile: \"writeFile\",\n  readRange: \"fsReadRange\",\n  writeRange: \"fsWriteRange\",\n  truncate: \"fsTruncate\",\n  utimes: \"utimes\",\n  chmod: \"chmod\",\n  access: \"access\",\n  chown: \"chown\",\n  open: \"fsOpen\",\n  read: \"fsRead\",\n  write: \"fsWrite\",\n  close: \"fsClose\",\n  readdir: \"readdir\",\n  mkdir: \"mkdir\",\n  unlink: \"unlink\",\n  rmdir: \"rmdir\",\n  rename: \"rename\",\n  readlink: \"readlink\",\n  symlink: \"symlink\",\n  fsync: \"fsSync\",\n  revision: \"fsRevision\",\n  acquire: \"fsAcquire\",\n  list: \"fsList\",\n  realpath: \"fsRealpath\",\n  remove: \"fsRemove\",\n  copyFile: \"fsCopyFile\",\n  fstat: \"fsFstat\",\n  dup: \"fsDup\",\n  seek: \"fsSeek\",\n  setStatus: \"fsSetStatus\",\n  readdirHandle: \"fsReaddirHandle\",\n  ftruncate: \"fsFtruncate\",\n  fchmod: \"fsFchmod\",\n  fchown: \"fsFchown\",\n  futimes: \"fsFutimes\",\n  appendOnce: \"fsAppend\",\n  acknowledgeAppend: \"fsAppendAck\",\n  writeBatch: \"writeBatch\",\n  writeStream: \"writeBatchStream\",\n  acquireExclusiveMutation: \"fsAcquireExclusiveMutation\",\n  releaseExclusiveMutation: \"fsReleaseExclusiveMutation\"\n};\nfunction vfsSupervisor(fs) {\n  return {\n    synchronous: fs.synchronous,\n    stat: (...args) => fs.stat(...args),\n    readFileBytes: (...args) => fs.readFile(...args),\n    writeFile: (...args) => fs.writeFile(...args),\n    fsReadRange: (...args) => fs.readRange(...args),\n    fsWriteRange: (...args) => fs.writeRange(...args),\n    fsTruncate: (...args) => fs.truncate(...args),\n    utimes: (...args) => fs.utimes(...args),\n    chmod: (...args) => fs.chmod(...args),\n    access: (...args) => fs.access(...args),\n    chown: (...args) => fs.chown(...args),\n    fsOpen: (...args) => fs.open(...args),\n    fsRead: (...args) => fs.read(...args),\n    fsWrite: (...args) => fs.write(...args),\n    fsClose: (...args) => fs.close(...args),\n    readdir: (...args) => fs.readdir(...args),\n    mkdir: (...args) => fs.mkdir(...args),\n    unlink: (...args) => fs.unlink(...args),\n    rmdir: (...args) => fs.rmdir(...args),\n    rename: (...args) => fs.rename(...args),\n    readlink: (...args) => fs.readlink(...args),\n    symlink: (...args) => fs.symlink(...args),\n    fsSync: (...args) => fs.fsync(...args),\n    fsRevision: (...args) => fs.revision(...args),\n    fsAcquire: (...args) => fs.acquire(...args),\n    fsList: (...args) => fs.list(...args),\n    fsRealpath: (...args) => fs.realpath(...args),\n    fsRemove: (...args) => fs.remove(...args),\n    fsCopyFile: (...args) => fs.copyFile(...args),\n    fsFstat: (...args) => fs.fstat(...args),\n    fsDup: (...args) => fs.dup(...args),\n    fsSeek: (...args) => fs.seek(...args),\n    fsSetStatus: (...args) => fs.setStatus(...args),\n    fsReaddirHandle: (...args) => fs.readdirHandle(...args),\n    fsFtruncate: (...args) => fs.ftruncate(...args),\n    fsFchmod: (...args) => fs.fchmod(...args),\n    fsFchown: (...args) => fs.fchown(...args),\n    fsFutimes: (...args) => fs.futimes(...args),\n    fsAppend: (...args) => fs.appendOnce(...args),\n    fsAppendAck: (...args) => fs.acknowledgeAppend(...args),\n    writeBatch: (...args) => fs.writeBatch(...args),\n    writeBatchStream: (...args) => fs.writeStream(...args),\n    fsAcquireExclusiveMutation: (...args) => fs.acquireExclusiveMutation(...args),\n    fsReleaseExclusiveMutation: (...args) => fs.releaseExclusiveMutation(...args)\n  };\n}\nfunction pending(result) {\n  return (typeof result === \"object\" || typeof result === \"function\") && result !== null && typeof result.then === \"function\";\n}\nfunction hop(result) {\n  return pending(result) ? Promise.resolve(result).catch(restoreCode) : result;\n}\nfunction bytes(result) {\n  return pending(result) ? Promise.resolve(result).catch(restoreCode).then(asBytes) : asBytes(result);\n}\nfunction asBytes(value) {\n  return value instanceof ArrayBuffer ? new Uint8Array(value) : value;\n}\nfunction restoreCode(error) {\n  if (error instanceof Error && !(\"code\" in error)) {\n    const code = /^([A-Z]+):/.exec(error.message)?.[1];\n    if (code)\n      throw Object.assign(error, { code });\n  }\n  throw error;\n}\nfunction supervisorFilesystem(supervisor, local) {\n  return {\n    synchronous: local,\n    stat: (...args) => hop(supervisor.stat(...args)),\n    readFile: (...args) => bytes(supervisor.readFileBytes(...args)),\n    writeFile: (...args) => hop(supervisor.writeFile(...args)),\n    readRange: (...args) => bytes(supervisor.fsReadRange(...args)),\n    writeRange: (...args) => hop(supervisor.fsWriteRange(...args)),\n    truncate: (...args) => hop(supervisor.fsTruncate(...args)),\n    utimes: (...args) => hop(supervisor.utimes(...args)),\n    chmod: (...args) => hop(supervisor.chmod(...args)),\n    access: (...args) => hop(supervisor.access(...args)),\n    chown: (...args) => hop(supervisor.chown(...args)),\n    open: (...args) => hop(supervisor.fsOpen(...args)),\n    read: (...args) => bytes(supervisor.fsRead(...args)),\n    write: (...args) => hop(supervisor.fsWrite(...args)),\n    close: (...args) => hop(supervisor.fsClose(...args)),\n    readdir: (...args) => hop(supervisor.readdir(...args)),\n    mkdir: (...args) => hop(supervisor.mkdir(...args)),\n    unlink: (...args) => hop(supervisor.unlink(...args)),\n    rmdir: (...args) => hop(supervisor.rmdir(...args)),\n    rename: (...args) => hop(supervisor.rename(...args)),\n    readlink: (...args) => hop(supervisor.readlink(...args)),\n    symlink: (...args) => hop(supervisor.symlink(...args)),\n    fsync: (...args) => hop(supervisor.fsSync(...args)),\n    revision: (...args) => hop(supervisor.fsRevision(...args)),\n    acquire: (...args) => hop(supervisor.fsAcquire(...args)),\n    list: (...args) => hop(supervisor.fsList(...args)),\n    realpath: (...args) => hop(supervisor.fsRealpath(...args)),\n    remove: (...args) => hop(supervisor.fsRemove(...args)),\n    copyFile: (...args) => hop(supervisor.fsCopyFile(...args)),\n    fstat: (...args) => hop(supervisor.fsFstat(...args)),\n    dup: (...args) => hop(supervisor.fsDup(...args)),\n    seek: (...args) => hop(supervisor.fsSeek(...args)),\n    setStatus: (...args) => hop(supervisor.fsSetStatus(...args)),\n    readdirHandle: (...args) => hop(supervisor.fsReaddirHandle(...args)),\n    ftruncate: (...args) => hop(supervisor.fsFtruncate(...args)),\n    fchmod: (...args) => hop(supervisor.fsFchmod(...args)),\n    fchown: (...args) => hop(supervisor.fsFchown(...args)),\n    futimes: (...args) => hop(supervisor.fsFutimes(...args)),\n    appendOnce: (...args) => hop(supervisor.fsAppend(...args)),\n    acknowledgeAppend: (...args) => hop(supervisor.fsAppendAck(...args)),\n    writeBatch: (...args) => hop(supervisor.writeBatch(...args)),\n    writeStream: (...args) => Promise.resolve(supervisor.writeBatchStream(...args)).catch(restoreCode),\n    acquireExclusiveMutation: (...args) => hop(supervisor.fsAcquireExclusiveMutation(...args)),\n    releaseExclusiveMutation: (...args) => hop(supervisor.fsReleaseExclusiveMutation(...args))\n  };\n}\n\nvar CHUNK_SIZE = 65536;\nvar MAX_TX_BLOB_BYTES = 1 * 1024 * 1024;\nvar MAX_TX_LOGICAL_ROWS = 256;\nvar MAX_TX_SQL_EXECS = 64;\nvar SQL_MAX_BOUND_PARAMETERS = 100;\nvar DO_STORAGE_LIMIT_BYTES = 1e10;\nvar SQLITE_MAX_STATEMENT_BYTES = 100 * 1024;\nvar SQLITE_MAX_BOUND_PARAMETERS = 100;\nvar SQLITE_MAX_ROW_BYTES = 2e6;\nvar BLOCK_CONCURRENCY_CANCEL_MS = 3e4;\nvar RESIDENT_KEEPALIVE_MS = 5e3;\nvar WS_ATTACHMENT_LIMIT_BYTES = 16384;\nvar MAX_RPC_SAFE_PAYLOAD_BYTES = 28 * 1024 * 1024;\nvar SUPERVISOR_HEAP_CEILING_BYTES = 64 * 1024 * 1024;\nvar SUPERVISOR_IN_FLIGHT_ALLOCATION_BUDGET_BYTES = 40 * 1024 * 1024;\nvar SUPERVISOR_READ_RESERVE_BYTES = 1024 * 1024;\nvar MAX_GLOBAL_WRITE_STREAM_CREDIT_BYTES = 8 * 1024 * 1024;\nvar PRE_BUNDLE_SLICE_CAP_BYTES = MAX_RPC_SAFE_PAYLOAD_BYTES;\nvar PRE_BUNDLE_CONCURRENCY = 1;\n\nvar NIMBUS_VERSION = \"2.0.0\";\nvar NODE_VERSION = \"v22.19.0\";\nvar NODE_VERSIONS = { node: \"22.19.0\", v8: \"12.4.254.21\", modules: \"127\" };\nvar ESBUILD_VERSION = \"0.24.2\";\nvar SQLJS_VERSION = \"1.14.1\";\nvar OPENCODE_VERSION = \"1.16.2\";\nvar LRU_MAX_ENTRIES = 512;\nvar BATCH_SIZE = 64;\nvar VFS_CAPACITY = 10 * 1024 * 1024 * 1024;\nvar FS_READ_BATCH_PATH_LIMIT = 1024;\nvar FS_READ_BATCH_REQUEST_BYTES = 4 * 1024 * 1024;\nvar FS_LIST_PAGE_LIMIT = 8192;\nvar VITE_MODULE_CACHE_MAX_ENTRIES = 1024;\nvar ON_DEMAND_SLICE_CAP_BYTES = MAX_RPC_SAFE_PAYLOAD_BYTES;\nvar VFS_BUNDLE_MAX_FILES = 4e3;\nvar VFS_BUNDLE_MAX_BYTES = 24 * 1024 * 1024;\nvar BUNDLE_MAX_ENCODED_BYTES = 22 * 1024 * 1024;\nvar PREFETCH_CACHE_MAX_BYTES = 10 * 1024 * 1024;\nvar ESM_TRANSFORM_CACHE_MAX_BYTES = 4 * 1024 * 1024;\nvar CWD_SNAPSHOT_MAX_FILE_BYTES = 2 * 1024 * 1024;\nvar RESIDENT_FILL_MAX_BYTES = 8 * 1024 * 1024;\nvar WASI_RESIDENT_FILE_CAP_BYTES = 8 * 1024 * 1024;\nvar NPM_REGISTRY = \"https://registry.npmjs.org\";\nvar NPM_CONCURRENCY = 12;\nvar NPM_DECOMPRESS_TIMEOUT = 15e3;\nvar DEFAULT_VITE_PORT = 5173;\nvar DEFAULT_PREVIEW_BASE = \"/preview\";\nvar DEFAULT_WORKER_BASE = \"/worker\";\nvar WRANGLER_DEBOUNCE_MS = 250;\nvar NIMBUS_AI_GATEWAY_PORT = 8790;\nvar CF_COMPAT_DATE = \"2026-04-01\";\nvar DEFAULT_HOSTNAME = \"nimbus\";\nvar DEFAULT_HOME = \"/home/user\";\nvar DEFAULT_USER = \"user\";\nvar DEFAULT_SHELL = \"/bin/sh\";\nvar DEFAULT_PATH = \"/usr/local/bin:/usr/bin:/bin:/home/user/.local/bin:/home/user/.gem/bin\";\nvar DEFAULT_MOUNT_POINTS = [\"bin\", \"etc\", \"home\", \"tmp\", \"var\", \"usr\", \"opt\"];\nvar FACET_PROVIDED_PACKAGE_ENTRYPOINTS = Object.freeze({ undici: \"index.js\" });\nvar FACET_PROVIDED_PACKAGES = Object.freeze(Object.keys(FACET_PROVIDED_PACKAGE_ENTRYPOINTS));\n\nvar __WASI_ESUCCESS = 0;\nvar __WASI_EAGAIN = 6;\nvar __WASI_EACCES = 2;\nvar __WASI_EBADF = 8;\nvar __WASI_ECONNREFUSED = 14;\nvar __WASI_EEXIST = 20;\nvar __WASI_EHOSTUNREACH = 23;\nvar __WASI_EINVAL = 28;\nvar __WASI_EIO = 29;\nvar __WASI_EISDIR = 31;\nvar __WASI_ELOOP = 32;\nvar __WASI_ENOENT = 44;\nvar __WASI_ENOSYS = 52;\nvar __WASI_ENOTCONN = 53;\nvar __WASI_ENOTDIR = 54;\nvar __WASI_ENOTEMPTY = 55;\nvar __WASI_ENOTSOCK = 57;\nvar __WASI_EPIPE = 64;\nvar __WASI_ESPIPE = 70;\nvar __WASI_ETIMEDOUT = 73;\nvar __WASI_ENOTCAPABLE = 76;\nvar __WASI_CLOCK_REALTIME = 0;\nvar __WASI_CLOCK_MONOTONIC = 1;\nvar __WASI_CLOCK_PROCESS_CPUTIME_ID = 2;\nvar __WASI_CLOCK_THREAD_CPUTIME_ID = 3;\nvar __WASI_O_CREAT = 1;\nvar __WASI_O_DIRECTORY = 2;\nvar __WASI_O_EXCL = 4;\nvar __WASI_O_TRUNC = 8;\nvar __WASI_LOOKUPFLAGS_SYMLINK_FOLLOW = 1;\nvar __WASI_FDFLAGS_APPEND = 1;\nvar __WASI_FDFLAGS_NONBLOCK = 4;\nvar __WASI_FSTFLAGS_ATIM = 1;\nvar __WASI_FSTFLAGS_ATIM_NOW = 2;\nvar __WASI_FSTFLAGS_MTIM = 4;\nvar __WASI_FSTFLAGS_MTIM_NOW = 8;\nvar __WASI_FT_UNKNOWN = 0;\nvar __WASI_FT_BLOCK_DEVICE = 1;\nvar __WASI_FT_CHARACTER_DEVICE = 2;\nvar __WASI_FT_DIRECTORY = 3;\nvar __WASI_FT_REGULAR_FILE = 4;\nvar __WASI_FT_SOCKET_DGRAM = 5;\nvar __WASI_FT_SOCKET_STREAM = 6;\nvar __WASI_FT_SYMBOLIC_LINK = 7;\nvar __WASI_PREOPENTYPE_DIR = 0;\nvar __WASI_SYMLOOP_MAX = 40;\nvar __WASI_RIGHTS_ALL = 0xFFFFFFFFFFFFFFFFn;\nvar __WASI_RIGHT_FD_READ = 1n << 1n;\nvar __WASI_RIGHT_FD_WRITE = 1n << 6n;\nvar __WASI_SDFLAGS_RD = 1;\nvar __WASI_SDFLAGS_WR = 2;\nvar __WASI_EVENTTYPE_CLOCK = 0;\nvar __WASI_EVENTTYPE_FD_READ = 1;\nvar __WASI_EVENTTYPE_FD_WRITE = 2;\nvar __WASI_SUBCLOCKFLAGS_ABSTIME = 1;\nvar __cfSocketConnect = null;\ntry {\n  const __mod = await import(\"cloudflare:sockets\");\n  __cfSocketConnect = __mod.connect;\n} catch (__e) {\n}\nvar __WasiExit = class {\n  constructor(code) {\n    this.code = code | 0;\n  }\n};\nvar __WASI_STREAM_DEV = 1n << 32n;\nfunction __wasiEmptyFS() {\n  return { root: \"\", residentFileCap: WASI_RESIDENT_FILE_CAP_BYTES };\n}\nvar __wasiFS = __wasiEmptyFS();\nvar __wasiPreopens = [];\nvar __wasiThreads = null;\nvar __wasiSup = null;\nfunction __wasiAdoptSupervisor(sup) {\n  if (sup) __wasiSup = sup;\n}\nfunction __wasiCanonicalize(p) {\n  if (!p) return \"\";\n  const parts = p.split(\"/\");\n  const out = [];\n  for (const seg of parts) {\n    if (seg === \"\" || seg === \".\") continue;\n    if (seg === \"..\") {\n      if (out.length > 0) out.pop();\n      continue;\n    }\n    out.push(seg);\n  }\n  return out.join(\"/\");\n}\nfunction __wasiInitFS(opts) {\n  __wasiSup = null;\n  __wasiFS = {\n    root: __wasiCanonicalize(opts.root || \"\"),\n    residentFileCap: Number(opts.residentFileCap ?? WASI_RESIDENT_FILE_CAP_BYTES)\n  };\n  __wasiPreopens = [];\n  fdTable.clear();\n  fdTable.set(0, { kind: \"stdin\" });\n  fdTable.set(1, { kind: \"stdout\" });\n  fdTable.set(2, { kind: \"stderr\" });\n  nextFd = 3;\n  for (const po of opts.preopens || []) {\n    const fd = __wasiAllocateFd();\n    const vfsPath = __wasiCanonicalize(po.vfsPath);\n    fdTable.set(fd, { kind: \"preopen\", wasiPath: po.wasiPath, vfsPath });\n    __wasiPreopens.push({ fd, wasiPath: po.wasiPath, vfsPath });\n  }\n}\nfunction __wasiGuestPath(baseFd, pathArg) {\n  if (pathArg.startsWith(\"/\")) return pathArg;\n  const base = fdTable.get(baseFd);\n  if (!base || base.kind !== \"preopen\") return pathArg;\n  return base.wasiPath.endsWith(\"/\") ? base.wasiPath + pathArg : base.wasiPath + \"/\" + pathArg;\n}\nfunction __wasiIsLoopbackHost(host) {\n  const h = host.toLowerCase().replace(/^\\[|\\]$/g, \"\");\n  return h === \"localhost\" || h === \"::1\" || h === \"::\" || h === \"0.0.0.0\" || /^127\\.\\d+\\.\\d+\\.\\d+$/.test(h);\n}\nglobalThis.__nimbusWasiLastSocketError = \"\";\nfunction __wasiConnectLoopback(port) {\n  const kernel = globalThis.__nimbusVirtualSockets;\n  if (!kernel || typeof kernel.connectStream !== \"function\") {\n    globalThis.__nimbusWasiLastSocketError = \"this process has no Nimbus virtual socket kernel, so in-session ports cannot be dialed\";\n    return { errno: __WASI_ENOSYS, socket: null };\n  }\n  try {\n    return { errno: __WASI_ESUCCESS, socket: kernel.connectStream(port) };\n  } catch (e) {\n    globalThis.__nimbusWasiLastSocketError = e && e.message ? e.message : String(e);\n    return { errno: __WASI_ECONNREFUSED, socket: null };\n  }\n}\nfunction __wasiConnectRemote(host, port) {\n  if (typeof __cfSocketConnect !== \"function\") {\n    globalThis.__nimbusWasiLastSocketError = \"outbound TCP is unavailable in this runtime\";\n    return { errno: __WASI_ENOSYS, socket: null };\n  }\n  try {\n    return { errno: __WASI_ESUCCESS, socket: __cfSocketConnect({ hostname: host, port }, { allowHalfOpen: true }) };\n  } catch (e) {\n    globalThis.__nimbusWasiLastSocketError = e && e.message ? e.message : String(e);\n    return { errno: __WASI_ECONNREFUSED, socket: null };\n  }\n}\nfunction __wasiOpenTcpSocket(pathArg, fdflags, fdOutPtr, writeU32LE) {\n  const tail = pathArg.substring(WASI_TCP_PATH_PREFIX.length);\n  const slashIdx = tail.lastIndexOf(\"/\");\n  if (slashIdx <= 0 || slashIdx === tail.length - 1) return __WASI_EINVAL;\n  const host = tail.substring(0, slashIdx);\n  const portStr = tail.substring(slashIdx + 1);\n  const port = parseInt(portStr, 10);\n  if (!host || !(port > 0 && port < 65536)) return __WASI_EINVAL;\n  const opened = __wasiIsLoopbackHost(host) ? __wasiConnectLoopback(port) : __wasiConnectRemote(host, port);\n  if (opened.errno !== __WASI_ESUCCESS) return opened.errno;\n  writeU32LE(fdOutPtr, __wasiAdoptSocket(opened.socket, fdflags));\n  return __WASI_ESUCCESS;\n}\nfunction __wasiKernelOrNull(what) {\n  const kernel = globalThis.__nimbusVirtualSockets;\n  if (kernel) return kernel;\n  globalThis.__nimbusWasiLastSocketError = \"this process has no Nimbus virtual socket kernel, so \" + what;\n  return null;\n}\nfunction __wasiOpenListener(pathArg, fdflags, fdOutPtr, writeU32LE) {\n  const port = parseInt(pathArg.substring(WASI_LISTEN_PATH_PREFIX.length), 10);\n  if (!Number.isInteger(port) || port <= 0 || port >= 65536) return __WASI_EINVAL;\n  const kernel = __wasiKernelOrNull(\"ports cannot be listened on\");\n  if (!kernel) return __WASI_ENOSYS;\n  if (!kernel.listeners.has(port)) {\n    try {\n      kernel.listen(port);\n    } catch (e) {\n      globalThis.__nimbusWasiLastSocketError = \"port \" + port + \" could not be bound: \" + (e && e.message ? e.message : String(e));\n      return __WASI_ENOTCONN;\n    }\n    const announce = Reflect.get(globalThis, \"__nimbusVirtualSocketDidListen\");\n    if (typeof announce === \"function\") announce(port);\n  }\n  const fd = __wasiAllocateFd();\n  fdTable.set(fd, { kind: \"listener\", port, fdflags: fdflags | 0 });\n  writeU32LE(fdOutPtr, fd);\n  return __WASI_ESUCCESS;\n}\nasync function __wasiAcceptRead(entry, iovsPtr, iovsLen, nreadPtr, writeU32LE, view, u8) {\n  const kernel = __wasiKernelOrNull(\"connections cannot be accepted\");\n  if (!kernel) return __WASI_ENOSYS;\n  try {\n    let accepted;\n    if ((entry.fdflags & __WASI_FDFLAGS_NONBLOCK) !== 0) {\n      accepted = kernel.acceptNow(entry.port);\n      if (!accepted) {\n        writeU32LE(nreadPtr, 0);\n        return __WASI_EAGAIN;\n      }\n    } else {\n      accepted = await kernel.accept(entry.port);\n    }\n    const bytes2 = new TextEncoder().encode(String(accepted.id) + \"\\n\");\n    const dv = view();\n    const memU8 = u8();\n    let total = 0;\n    for (let i = 0; i < iovsLen && total < bytes2.length; i++) {\n      const iov = iovsPtr + i * 8;\n      const bufPtr = dv.getUint32(iov, true);\n      const bufLen = dv.getUint32(iov + 4, true);\n      const n = Math.min(bufLen, bytes2.length - total);\n      memU8.set(bytes2.subarray(total, total + n), bufPtr);\n      total += n;\n    }\n    writeU32LE(nreadPtr, total);\n    return __WASI_ESUCCESS;\n  } catch (e) {\n    globalThis.__nimbusWasiLastSocketError = e && e.message ? e.message : String(e);\n    return __WASI_ENOTCONN;\n  }\n}\nfunction __wasiOpenAcceptedSocket(pathArg, fdflags, fdOutPtr, writeU32LE) {\n  const id = parseInt(pathArg.substring(WASI_ACCEPTED_PATH_PREFIX.length), 10);\n  if (!Number.isInteger(id) || id <= 0) return __WASI_EINVAL;\n  const kernel = globalThis.__nimbusVirtualSockets;\n  if (!kernel || typeof kernel.streamFor !== \"function\") {\n    globalThis.__nimbusWasiLastSocketError = \"this process has no Nimbus virtual socket kernel, so accepted connections cannot be bound\";\n    return __WASI_ENOSYS;\n  }\n  let socket;\n  try {\n    socket = kernel.streamFor(id);\n  } catch (e) {\n    globalThis.__nimbusWasiLastSocketError = e && e.message ? e.message : String(e);\n    return __WASI_ENOTCONN;\n  }\n  writeU32LE(fdOutPtr, __wasiAdoptSocket(socket, fdflags));\n  return __WASI_ESUCCESS;\n}\nfunction __wasiAdoptSocket(socket, fdflags) {\n  const fd = __wasiAllocateFd();\n  fdTable.set(fd, {\n    kind: \"socket\",\n    socket,\n    reader: null,\n    writer: null,\n    readBuf: new Uint8Array(0),\n    readBufOffset: 0,\n    eof: false,\n    closed: false,\n    halfClosedWr: false,\n    fdflags: fdflags | 0\n  });\n  return fd;\n}\nvar fdTable =   new Map();\nvar nextFd = 3;\nfunction __wasiAllocateFd() {\n  return nextFd++;\n}\nvar __WASI_PARK_CEILING_MS = 15e3;\nvar __WASI_PARK_DEADLINE_MS = 1e4;\nvoid __WASI_PARK_CEILING_MS;\nfunction withParkDeadline(fn) {\n  return function parkGuarded(...args) {\n    const r = fn.apply(this, args);\n    if (!r || typeof r.then !== \"function\") return r;\n    const guarded = new Promise((resolve) => {\n      let settled = false;\n      const timer = setTimeout(() => {\n        if (settled) return;\n        settled = true;\n        resolve(__WASI_EAGAIN);\n      }, __WASI_PARK_DEADLINE_MS);\n      const finish = (value) => {\n        if (settled) return;\n        settled = true;\n        clearTimeout(timer);\n        resolve(value);\n      };\n      r.then(finish, () => finish(__WASI_EAGAIN));\n    });\n    return __wasiThreads ? __wasiThreads.parkIo(guarded) : guarded;\n  };\n}\nfunction __wasiMakeImports(opts) {\n  const preview0 = opts.abi === \"preview0\";\n  const WHENCE_SET = preview0 ? 2 : 0;\n  const WHENCE_CUR = preview0 ? 0 : 1;\n  const WHENCE_END = preview0 ? 1 : 2;\n  const argv = opts.argv || [];\n  const envArr = [];\n  if (opts.env) for (const k of Object.keys(opts.env)) envArr.push(k + \"=\" + opts.env[k]);\n  const utf8enc = new TextEncoder();\n  const utf8dec = new TextDecoder();\n  function view() {\n    return new DataView(opts.getMemory().buffer);\n  }\n  function u8() {\n    return new Uint8Array(opts.getMemory().buffer);\n  }\n  function writeU32LE(off, v) {\n    view().setUint32(off, v >>> 0, true);\n  }\n  function writeU64LE(off, v) {\n    const dv = view();\n    if (typeof v === \"bigint\") {\n      dv.setBigUint64(off, v, true);\n      return;\n    }\n    const lo = v >>> 0;\n    const hi = Math.floor(v / 4294967296) >>> 0;\n    dv.setUint32(off, lo, true);\n    dv.setUint32(off + 4, hi, true);\n  }\n  function readPath(ptr, len) {\n    const bytes2 = u8().subarray(ptr, ptr + len);\n    return utf8dec.decode(bytes2);\n  }\n  let stdoutBuf = \"\";\n  let stderrBuf = \"\";\n  function appendStream(kind, bytes2) {\n    const s = utf8dec.decode(bytes2);\n    if (kind === \"stdout\") {\n      if (opts.stdoutWrite) opts.stdoutWrite(s);\n      else stdoutBuf += s;\n    } else {\n      if (opts.stderrWrite) opts.stderrWrite(s);\n      else stderrBuf += s;\n    }\n  }\n  function scatterIovs(bytes2, iovsPtr, iovsLen, dv, memU8) {\n    let done = 0;\n    for (let i = 0; i < iovsLen && done < bytes2.length; i++) {\n      const iov = iovsPtr + i * 8;\n      const bufPtr = dv.getUint32(iov, true);\n      const bufLen = dv.getUint32(iov + 4, true);\n      const n = Math.min(bufLen, bytes2.length - done);\n      if (n <= 0) break;\n      memU8.set(bytes2.subarray(done, done + n), bufPtr);\n      done += n;\n      if (n < bufLen) break;\n    }\n    return done;\n  }\n  function iovsCapacity(iovsPtr, iovsLen, dv) {\n    let total = 0;\n    for (let i = 0; i < iovsLen; i++) total += dv.getUint32(iovsPtr + i * 8 + 4, true);\n    return total;\n  }\n  const imports = {\n    args_get(argvPtr, argvBufPtr) {\n      let buf = argvBufPtr;\n      const memU8 = u8();\n      const dv = view();\n      for (let i = 0; i < argv.length; i++) {\n        dv.setUint32(argvPtr + i * 4, buf, true);\n        const bytes2 = utf8enc.encode(argv[i] + \"\\0\");\n        memU8.set(bytes2, buf);\n        buf += bytes2.length;\n      }\n      return __WASI_ESUCCESS;\n    },\n    args_sizes_get(argcPtr, sizePtr) {\n      let total = 0;\n      for (let i = 0; i < argv.length; i++) total += utf8enc.encode(argv[i]).length + 1;\n      writeU32LE(argcPtr, argv.length);\n      writeU32LE(sizePtr, total);\n      return __WASI_ESUCCESS;\n    },\n    environ_get(environPtr, envBufPtr) {\n      let buf = envBufPtr;\n      const memU8 = u8();\n      const dv = view();\n      for (let i = 0; i < envArr.length; i++) {\n        dv.setUint32(environPtr + i * 4, buf, true);\n        const bytes2 = utf8enc.encode(envArr[i] + \"\\0\");\n        memU8.set(bytes2, buf);\n        buf += bytes2.length;\n      }\n      return __WASI_ESUCCESS;\n    },\n    environ_sizes_get(envcPtr, sizePtr) {\n      let total = 0;\n      for (let i = 0; i < envArr.length; i++) total += utf8enc.encode(envArr[i]).length + 1;\n      writeU32LE(envcPtr, envArr.length);\n      writeU32LE(sizePtr, total);\n      return __WASI_ESUCCESS;\n    },\n    fd_close(fd) {\n      if (fd === 0 || fd === 1 || fd === 2) return __WASI_ESUCCESS;\n      if (!fdTable.has(fd)) return __WASI_EBADF;\n      const entry = fdTable.get(fd);\n      if (entry.kind === \"preopen\") return __WASI_ESUCCESS;\n      if (entry.kind === \"socket\" && !entry.closed) {\n        try {\n          entry.socket.close();\n        } catch {\n        }\n        entry.closed = true;\n      }\n      fdTable.delete(fd);\n      return __WASI_ESUCCESS;\n    },\n    fd_read(fd, iovsPtr, iovsLen, nreadPtr) {\n      if (fd === 0) {\n        writeU32LE(nreadPtr, 0);\n        return __WASI_ESUCCESS;\n      }\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      if (entry.kind === \"socket\") return __rawSockRecv(fd, iovsPtr, iovsLen, 0, nreadPtr, 0);\n      if (entry.kind === \"listener\") {\n        return __wasiAcceptRead(entry, iovsPtr, iovsLen, nreadPtr, writeU32LE, view, u8);\n      }\n      if (entry.kind === \"preopen\") return __WASI_EISDIR;\n      return __WASI_EBADF;\n    },\n    fd_write(fd, iovsPtr, iovsLen, nwrittenPtr) {\n      const sockEntry = fdTable.get(fd);\n      if (sockEntry && sockEntry.kind === \"socket\") {\n        return __rawSockSend(fd, iovsPtr, iovsLen, 0, nwrittenPtr);\n      }\n      if (fd !== 1 && fd !== 2) return __WASI_EBADF;\n      const dv = view();\n      const memU8 = u8();\n      let total = 0;\n      const parts = [];\n      for (let i = 0; i < iovsLen; i++) {\n        const iov = iovsPtr + i * 8;\n        const bufPtr = dv.getUint32(iov, true);\n        const bufLen = dv.getUint32(iov + 4, true);\n        if (bufLen > 0) parts.push(memU8.slice(bufPtr, bufPtr + bufLen));\n        total += bufLen;\n      }\n      let combined;\n      if (parts.length === 0) combined = new Uint8Array(0);\n      else if (parts.length === 1) combined = parts[0];\n      else {\n        combined = new Uint8Array(total);\n        let off = 0;\n        for (const p of parts) {\n          combined.set(p, off);\n          off += p.length;\n        }\n      }\n      appendStream(fd === 1 ? \"stdout\" : \"stderr\", combined);\n      writeU32LE(nwrittenPtr, total);\n      return __WASI_ESUCCESS;\n    },\n    fd_seek(fd, _offsetArg, _whence, _newOffsetPtr) {\n      if (fd === 0 || fd === 1 || fd === 2) return __WASI_ESPIPE;\n      const entry = fdTable.get(fd);\n      if (entry && (entry.kind === \"socket\" || entry.kind === \"listener\")) return __WASI_ESPIPE;\n      return __WASI_EBADF;\n    },\n    fd_tell(fd, _offsetPtr) {\n      if (fd === 0 || fd === 1 || fd === 2) return __WASI_ESPIPE;\n      const entry = fdTable.get(fd);\n      if (entry && (entry.kind === \"socket\" || entry.kind === \"listener\")) return __WASI_ESPIPE;\n      return __WASI_EBADF;\n    },\n    fd_fdstat_get(fd, statPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      const dv = view();\n      let ftype = __WASI_FT_UNKNOWN;\n      if (entry.kind === \"stdin\" || entry.kind === \"stdout\" || entry.kind === \"stderr\") {\n        ftype = __WASI_FT_CHARACTER_DEVICE;\n      } else if (entry.kind === \"preopen\") {\n        ftype = __WASI_FT_DIRECTORY;\n      } else if (entry.kind === \"socket\" || entry.kind === \"listener\") {\n        ftype = __WASI_FT_SOCKET_STREAM;\n      }\n      dv.setUint8(statPtr, ftype);\n      dv.setUint8(statPtr + 1, 0);\n      dv.setUint16(statPtr + 2, (entry.fdflags || 0) & 65535, true);\n      dv.setUint32(statPtr + 4, 0, true);\n      const rb = entry.rights !== void 0 ? entry.rights : __WASI_RIGHTS_ALL;\n      const ri = entry.rightsInheriting !== void 0 ? entry.rightsInheriting : __WASI_RIGHTS_ALL;\n      writeU64LE(statPtr + 8, rb);\n      writeU64LE(statPtr + 16, ri);\n      return __WASI_ESUCCESS;\n    },\n    fd_fdstat_set_flags(fd, flags) {\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      if (entry.kind === \"socket\" || entry.kind === \"listener\") entry.fdflags = flags;\n      return __WASI_ESUCCESS;\n    },\n    fd_fdstat_set_rights(fd, rightsBase, rightsInheriting) {\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      const rb = typeof rightsBase === \"bigint\" ? rightsBase : BigInt(rightsBase >>> 0);\n      const ri = typeof rightsInheriting === \"bigint\" ? rightsInheriting : BigInt(rightsInheriting >>> 0);\n      const curRb = entry.rights !== void 0 ? entry.rights : __WASI_RIGHTS_ALL;\n      const curRi = entry.rightsInheriting !== void 0 ? entry.rightsInheriting : __WASI_RIGHTS_ALL;\n      if ((rb & ~curRb) !== 0n) return __WASI_ENOTCAPABLE;\n      if ((ri & ~curRi) !== 0n) return __WASI_ENOTCAPABLE;\n      entry.rights = rb;\n      entry.rightsInheriting = ri;\n      return __WASI_ESUCCESS;\n    },\n    fd_prestat_get(fd, prestatPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry || entry.kind !== \"preopen\") return __WASI_EBADF;\n      const dv = view();\n      dv.setUint8(prestatPtr, __WASI_PREOPENTYPE_DIR);\n      const nameBytes = utf8enc.encode(entry.wasiPath);\n      dv.setUint32(prestatPtr + 4, nameBytes.length, true);\n      return __WASI_ESUCCESS;\n    },\n    fd_prestat_dir_name(fd, pathPtr, pathLen) {\n      const entry = fdTable.get(fd);\n      if (!entry || entry.kind !== \"preopen\") return __WASI_EBADF;\n      const nameBytes = utf8enc.encode(entry.wasiPath);\n      if (nameBytes.length > pathLen) return __WASI_EINVAL;\n      u8().set(nameBytes, pathPtr);\n      return __WASI_ESUCCESS;\n    },\n    path_open(baseFd, dirflags, pathPtr, pathLen, oflags, rightsBase, _rightsInheriting, fdflags, fdOutPtr) {\n      const pathArg = readPath(pathPtr, pathLen);\n      const guestPath = __wasiGuestPath(baseFd, pathArg);\n      if (guestPath.startsWith(WASI_TCP_PATH_PREFIX)) {\n        return __wasiOpenTcpSocket(guestPath, fdflags, fdOutPtr, writeU32LE);\n      }\n      if (guestPath.startsWith(WASI_ACCEPTED_PATH_PREFIX)) {\n        return __wasiOpenAcceptedSocket(guestPath, fdflags, fdOutPtr, writeU32LE);\n      }\n      if (guestPath.startsWith(WASI_LISTEN_PATH_PREFIX)) {\n        return __wasiOpenListener(guestPath, fdflags, fdOutPtr, writeU32LE);\n      }\n      return __WASI_EBADF;\n    },\n    path_create_directory(_baseFd, _pathPtr, _pathLen) {\n      return __WASI_EBADF;\n    },\n    path_remove_directory(_baseFd, _pathPtr, _pathLen) {\n      return __WASI_EBADF;\n    },\n    path_unlink_file(_baseFd, _pathPtr, _pathLen) {\n      return __WASI_EBADF;\n    },\n    path_rename(_srcFd, _srcPathPtr, _srcPathLen, _dstFd, _dstPathPtr, _dstPathLen) {\n      return __WASI_EBADF;\n    },\n    path_filestat_get(_baseFd, _lookupflags, _pathPtr, _pathLen, _statPtr) {\n      return __WASI_EBADF;\n    },\n    path_filestat_set_times(_baseFd, _lookupflags, _pathPtr, _pathLen, _atimArg, _mtimArg, _fstflags) {\n      return __WASI_EBADF;\n    },\n    path_readlink(_baseFd, _pathPtr, _pathLen, _bufPtr, _bufLen, _bufUsedPtr) {\n      return __WASI_EBADF;\n    },\n    path_symlink(_oldPathPtr, _oldPathLen, _newFd, _newPathPtr, _newPathLen) {\n      return __WASI_EBADF;\n    },\n    path_link(_oldFd, _oldFlags, _oldPathPtr, _oldPathLen, _newFd, _newPathPtr, _newPathLen) {\n      return __WASI_EBADF;\n    },\n    fd_filestat_get(fd, statPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      let ftype = __WASI_FT_UNKNOWN;\n      if (entry.kind === \"stdin\" || entry.kind === \"stdout\" || entry.kind === \"stderr\") {\n        ftype = __WASI_FT_CHARACTER_DEVICE;\n      } else if (entry.kind === \"socket\" || entry.kind === \"listener\") {\n        ftype = __WASI_FT_SOCKET_STREAM;\n      } else return __WASI_EBADF;\n      const dv = view();\n      dv.setUint8(statPtr + 16, ftype);\n      writeU64LE(statPtr, __WASI_STREAM_DEV);\n      writeU64LE(statPtr + 8, BigInt(fd) + 1n);\n      if (preview0) {\n        for (let i = 17; i < 20; i++) dv.setUint8(statPtr + i, 0);\n        dv.setUint32(statPtr + 20, 1, true);\n        for (let off = 24; off < 56; off += 8) writeU64LE(statPtr + off, 0n);\n        return __WASI_ESUCCESS;\n      }\n      for (let i = 17; i < 24; i++) dv.setUint8(statPtr + i, 0);\n      writeU64LE(statPtr + 24, 1n);\n      for (let off = 32; off < 64; off += 8) writeU64LE(statPtr + off, 0n);\n      return __WASI_ESUCCESS;\n    },\n    fd_filestat_set_size(_fd, _size) {\n      return __WASI_EBADF;\n    },\n    fd_filestat_set_times(_fd, _atimArg, _mtimArg, _fstflags) {\n      return __WASI_EBADF;\n    },\n    fd_pread(_fd, _iovsPtr, _iovsLen, _offsetArg, _nreadPtr) {\n      return __WASI_EBADF;\n    },\n    fd_pwrite(_fd, _iovsPtr, _iovsLen, _offsetArg, _nwrittenPtr) {\n      return __WASI_EBADF;\n    },\n    fd_readdir(_fd, _bufPtr, _bufLen, _cookieArg, _bufusedPtr) {\n      return __WASI_EBADF;\n    },\n    fd_advise(_fd, _offset, _len, _advice) {\n      return __WASI_ESUCCESS;\n    },\n    fd_allocate(_fd, _offsetArg, _lenArg) {\n      return __WASI_EBADF;\n    },\n    fd_datasync() {\n      return __WASI_ESUCCESS;\n    },\n    fd_sync() {\n      return __WASI_ESUCCESS;\n    },\n    fd_renumber(from, to) {\n      const entry = fdTable.get(from);\n      if (!entry) return __WASI_EBADF;\n      if (from === to) return __WASI_ESUCCESS;\n      const target = fdTable.get(to);\n      if (target && target.kind === \"preopen\") return __WASI_ENOTCAPABLE;\n      if (target && target.kind === \"socket\" && !target.closed) {\n        try {\n          target.socket.close();\n        } catch {\n        }\n        target.closed = true;\n      }\n      fdTable.delete(from);\n      fdTable.set(to, entry);\n      return __WASI_ESUCCESS;\n    },\n    proc_exit(code) {\n      throw new __WasiExit(code | 0);\n    },\n    proc_raise(sig) {\n      throw new __WasiExit(128 + ((sig | 0) & 255));\n    },\n    clock_time_get(clockId, _precision, timePtr) {\n      let nowNs;\n      if (clockId === __WASI_CLOCK_REALTIME) {\n        nowNs = BigInt(Date.now()) * 1000000n;\n      } else if (clockId === __WASI_CLOCK_MONOTONIC || clockId === __WASI_CLOCK_PROCESS_CPUTIME_ID || clockId === __WASI_CLOCK_THREAD_CPUTIME_ID) {\n        const ms = typeof performance !== \"undefined\" && performance.now ? performance.now() : Date.now();\n        nowNs = BigInt(Math.floor(ms * 1e3)) * 1000n;\n      } else return __WASI_EINVAL;\n      writeU64LE(timePtr, nowNs);\n      return __WASI_ESUCCESS;\n    },\n    clock_res_get(clockId, resPtr) {\n      if (clockId === __WASI_CLOCK_REALTIME) {\n        writeU64LE(resPtr, 1000000n);\n        return __WASI_ESUCCESS;\n      }\n      if (clockId === __WASI_CLOCK_MONOTONIC || clockId === __WASI_CLOCK_PROCESS_CPUTIME_ID || clockId === __WASI_CLOCK_THREAD_CPUTIME_ID) {\n        writeU64LE(resPtr, 1000n);\n        return __WASI_ESUCCESS;\n      }\n      return __WASI_EINVAL;\n    },\n    random_get(bufPtr, bufLen) {\n      const memU8 = u8();\n      const CHUNK = 65536;\n      let off = 0;\n      while (off < bufLen) {\n        const n = Math.min(bufLen - off, CHUNK);\n        crypto.getRandomValues(memU8.subarray(bufPtr + off, bufPtr + off + n));\n        off += n;\n      }\n      return __WASI_ESUCCESS;\n    },\n    sched_yield() {\n      return __wasiThreads ? __wasiThreads.yieldNow() : __WASI_ESUCCESS;\n    },\n    async poll_oneoff(inSubsPtr, outEventsPtr, nsubs, retNeventsPtr) {\n      const dv = view();\n      if ((nsubs | 0) <= 0) {\n        writeU32LE(retNeventsPtr, 0);\n        return __WASI_ESUCCESS;\n      }\n      const subs = [];\n      for (let i = 0; i < nsubs; i++) {\n        const base = inSubsPtr + i * 48;\n        const userdata = dv.getBigUint64(base, true);\n        const tag = dv.getUint8(base + 8);\n        if (tag === __WASI_EVENTTYPE_CLOCK) {\n          const id = dv.getUint32(base + 16, true);\n          const timeout = dv.getBigUint64(base + 24, true);\n          const precision = dv.getBigUint64(base + 32, true);\n          const flags = dv.getUint16(base + 40, true);\n          subs.push({ idx: i, userdata, tag, id, timeout, precision, flags });\n        } else if (tag === __WASI_EVENTTYPE_FD_READ || tag === __WASI_EVENTTYPE_FD_WRITE) {\n          const fd = dv.getUint32(base + 16, true);\n          subs.push({ idx: i, userdata, tag, fd });\n        } else {\n          subs.push({ idx: i, userdata, tag, badTag: true });\n        }\n      }\n      const timerIds = new Array(subs.length).fill(null);\n      const readerLocks = [];\n      const monoNowNs = () => {\n        const ms = typeof performance !== \"undefined\" && performance.now ? performance.now() : Date.now();\n        return BigInt(Math.floor(ms * 1e3)) * 1000n;\n      };\n      const promises = subs.map((s) => {\n        if (s.badTag) {\n          return Promise.resolve({\n            idx: s.idx,\n            error: __WASI_EINVAL,\n            type: s.tag,\n            nbytes: 0n,\n            flags: 0\n          });\n        }\n        if (s.tag === __WASI_EVENTTYPE_CLOCK) {\n          let deadlineNs;\n          if ((s.flags & __WASI_SUBCLOCKFLAGS_ABSTIME) !== 0) {\n            deadlineNs = s.timeout;\n          } else {\n            const nowNs = s.id === __WASI_CLOCK_REALTIME ? BigInt(Date.now()) * 1000000n : monoNowNs();\n            deadlineNs = nowNs + s.timeout;\n          }\n          const nowAgain = s.id === __WASI_CLOCK_REALTIME ? BigInt(Date.now()) * 1000000n : monoNowNs();\n          const remainNs = deadlineNs > nowAgain ? deadlineNs - nowAgain : 0n;\n          const remainMs = Number(remainNs / 1000000n);\n          return new Promise((resolve) => {\n            const t = setTimeout(() => resolve({\n              idx: s.idx,\n              error: __WASI_ESUCCESS,\n              type: s.tag,\n              nbytes: 0n,\n              flags: 0\n            }), remainMs > 0 ? remainMs : 0);\n            timerIds[s.idx] = t;\n          });\n        }\n        const entry = fdTable.get(s.fd);\n        if (!entry) {\n          return Promise.resolve({\n            idx: s.idx,\n            error: __WASI_EBADF,\n            type: s.tag,\n            nbytes: 0n,\n            flags: 0\n          });\n        }\n        if (entry.kind === \"preopen\" || entry.kind === \"authority\" || entry.kind === \"resident\" || entry.kind === \"stdin\" || entry.kind === \"stdout\" || entry.kind === \"stderr\") {\n          let nbytes = 0n;\n          if (entry.kind === \"resident\" && s.tag === __WASI_EVENTTYPE_FD_READ) {\n            const remain = entry.bytes.length - entry.position;\n            nbytes = remain > 0 ? BigInt(remain) : 0n;\n          } else if (s.tag === __WASI_EVENTTYPE_FD_WRITE) {\n            nbytes = 0xFFFFFFFFn;\n          }\n          return Promise.resolve({\n            idx: s.idx,\n            error: __WASI_ESUCCESS,\n            type: s.tag,\n            nbytes,\n            flags: 0\n          });\n        }\n        if (entry.kind === \"listener\") {\n          if (s.tag === __WASI_EVENTTYPE_FD_WRITE) {\n            return Promise.resolve({ idx: s.idx, error: __WASI_ESUCCESS, type: s.tag, nbytes: 0n, flags: 0 });\n          }\n          const kernel = globalThis.__nimbusVirtualSockets;\n          if (!kernel) {\n            return Promise.resolve({ idx: s.idx, error: __WASI_ENOSYS, type: s.tag, nbytes: 0n, flags: 0 });\n          }\n          const ready2 = () => ({\n            idx: s.idx,\n            error: __WASI_ESUCCESS,\n            type: s.tag,\n            nbytes: BigInt(kernel.pending(entry.port)),\n            flags: 0\n          });\n          if (kernel.pending(entry.port) > 0) return Promise.resolve(ready2());\n          return kernel.waitReadable([entry.port], null).then(ready2);\n        }\n        if (entry.kind === \"socket\") {\n          if (s.tag === __WASI_EVENTTYPE_FD_WRITE) {\n            return Promise.resolve({\n              idx: s.idx,\n              error: __WASI_ESUCCESS,\n              type: s.tag,\n              nbytes: 0xFFFFFFFFn,\n              flags: 0\n            });\n          }\n          if (entry.readBuf && entry.readBufOffset < entry.readBuf.length) {\n            const avail = BigInt(entry.readBuf.length - entry.readBufOffset);\n            return Promise.resolve({\n              idx: s.idx,\n              error: __WASI_ESUCCESS,\n              type: s.tag,\n              nbytes: avail,\n              flags: 0\n            });\n          }\n          if (entry.eof || entry.closed) {\n            return Promise.resolve({\n              idx: s.idx,\n              error: __WASI_ESUCCESS,\n              type: s.tag,\n              nbytes: 0n,\n              flags: 1\n            });\n          }\n          return (async () => {\n            try {\n              if (!entry.reader) entry.reader = entry.socket.readable.getReader();\n              await entry.socket.opened;\n              const { value, done } = await entry.reader.read();\n              if (done) {\n                entry.eof = true;\n                entry.readBuf = new Uint8Array(0);\n                entry.readBufOffset = 0;\n                return {\n                  idx: s.idx,\n                  error: __WASI_ESUCCESS,\n                  type: s.tag,\n                  nbytes: 0n,\n                  flags: 1\n                };\n              }\n              entry.readBuf = value instanceof Uint8Array ? value : new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n              entry.readBufOffset = 0;\n              return {\n                idx: s.idx,\n                error: __WASI_ESUCCESS,\n                type: s.tag,\n                nbytes: BigInt(entry.readBuf.length),\n                flags: 0\n              };\n            } catch (e) {\n              return {\n                idx: s.idx,\n                error: __WASI_EIO,\n                type: s.tag,\n                nbytes: 0n,\n                flags: 0\n              };\n            }\n          })();\n        }\n        return Promise.resolve({\n          idx: s.idx,\n          error: __WASI_EBADF,\n          type: s.tag,\n          nbytes: 0n,\n          flags: 0\n        });\n      });\n      const winnerResult = await Promise.race(promises);\n      const ready = [];\n      ready.push(winnerResult);\n      const sentinel = Promise.resolve(\"__not-ready__\");\n      for (let i = 0; i < promises.length; i++) {\n        if (i === winnerResult.idx) continue;\n        const p = promises[i];\n        const probed = await Promise.race([p, sentinel]);\n        if (probed !== \"__not-ready__\") {\n          ready.push(probed);\n        } else {\n          if (timerIds[i] !== null) {\n            try {\n              clearTimeout(timerIds[i]);\n            } catch {\n            }\n            timerIds[i] = null;\n          }\n        }\n      }\n      let nevents = 0;\n      for (const ev of ready) {\n        const off = outEventsPtr + nevents * 32;\n        const subUserdata = subs[ev.idx].userdata;\n        dv.setBigUint64(off, subUserdata, true);\n        dv.setUint16(off + 8, ev.error, true);\n        dv.setUint8(off + 10, ev.type);\n        for (let p = 11; p < 16; p++) dv.setUint8(off + p, 0);\n        dv.setBigUint64(off + 16, ev.nbytes || 0n, true);\n        dv.setUint16(off + 24, ev.flags || 0, true);\n        for (let p = 26; p < 32; p++) dv.setUint8(off + p, 0);\n        nevents++;\n      }\n      writeU32LE(retNeventsPtr, nevents);\n      return __WASI_ESUCCESS;\n    },\n    async sock_send(fd, siDataPtr, siDataLen, _siFlags, retDataLenPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry || entry.kind !== \"socket\") return __WASI_ENOTSOCK;\n      if (entry.closed || entry.halfClosedWr) return __WASI_EPIPE;\n      const dv = view();\n      const memU8 = u8();\n      let total = 0;\n      const parts = [];\n      for (let i = 0; i < siDataLen; i++) {\n        const iov = siDataPtr + i * 8;\n        const bufPtr = dv.getUint32(iov, true);\n        const bufLen = dv.getUint32(iov + 4, true);\n        if (bufLen > 0) parts.push(memU8.slice(bufPtr, bufPtr + bufLen));\n        total += bufLen;\n      }\n      let combined;\n      if (parts.length === 0) combined = new Uint8Array(0);\n      else if (parts.length === 1) combined = parts[0];\n      else {\n        combined = new Uint8Array(total);\n        let off = 0;\n        for (const p of parts) {\n          combined.set(p, off);\n          off += p.length;\n        }\n      }\n      try {\n        if (!entry.writer) entry.writer = entry.socket.writable.getWriter();\n        await entry.socket.opened;\n        await entry.writer.write(combined);\n      } catch (e) {\n        const msg = e && e.message ? e.message : String(e);\n        globalThis.__nimbusWasiLastSocketError = msg;\n        if (/refused|ECONNREFUSED/i.test(msg)) return __WASI_ECONNREFUSED;\n        if (/unreach|EHOSTUNREACH/i.test(msg)) return __WASI_EHOSTUNREACH;\n        return __WASI_EIO;\n      }\n      writeU32LE(retDataLenPtr, total);\n      return __WASI_ESUCCESS;\n    },\n    async sock_recv(fd, riDataPtr, riDataLen, _riFlags, retDataLenPtr, retFlagsPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry || entry.kind !== \"socket\") return __WASI_ENOTSOCK;\n      if (entry.closed) return __WASI_ENOTCONN;\n      const dv = view();\n      const memU8 = u8();\n      if (entry.readBufOffset >= entry.readBuf.length && !entry.eof) {\n        try {\n          if (!entry.reader) entry.reader = entry.socket.readable.getReader();\n          await entry.socket.opened;\n          const { value, done } = await entry.reader.read();\n          if (done) {\n            entry.eof = true;\n            entry.readBuf = new Uint8Array(0);\n            entry.readBufOffset = 0;\n          } else {\n            entry.readBuf = value instanceof Uint8Array ? value : new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n            entry.readBufOffset = 0;\n          }\n        } catch (e) {\n          const msg = e && e.message ? e.message : String(e);\n          if (/refused|ECONNREFUSED/i.test(msg)) return __WASI_ECONNREFUSED;\n          if (/unreach|EHOSTUNREACH/i.test(msg)) return __WASI_EHOSTUNREACH;\n          return __WASI_EIO;\n        }\n      }\n      let total = 0;\n      for (let i = 0; i < riDataLen; i++) {\n        const iov = riDataPtr + i * 8;\n        const bufPtr = dv.getUint32(iov, true);\n        const bufLen = dv.getUint32(iov + 4, true);\n        const remain = entry.readBuf.length - entry.readBufOffset;\n        if (remain <= 0) break;\n        const n = Math.min(bufLen, remain);\n        memU8.set(entry.readBuf.subarray(entry.readBufOffset, entry.readBufOffset + n), bufPtr);\n        entry.readBufOffset += n;\n        total += n;\n        if (n < bufLen) break;\n      }\n      writeU32LE(retDataLenPtr, total);\n      if (typeof retFlagsPtr === \"number\") {\n        dv.setUint16(retFlagsPtr, 0, true);\n      }\n      return __WASI_ESUCCESS;\n    },\n    async sock_shutdown(fd, how) {\n      const entry = fdTable.get(fd);\n      if (!entry || entry.kind !== \"socket\") return __WASI_ENOTSOCK;\n      const wantRd = (how & __WASI_SDFLAGS_RD) !== 0;\n      const wantWr = (how & __WASI_SDFLAGS_WR) !== 0;\n      try {\n        if (wantRd && wantWr) {\n          if (entry.writer) {\n            try {\n              await entry.writer.close();\n            } catch {\n            }\n          }\n          if (entry.reader) {\n            try {\n              await entry.reader.cancel();\n            } catch {\n            }\n          }\n          try {\n            await entry.socket.close();\n          } catch {\n          }\n          entry.halfClosedWr = true;\n          entry.eof = true;\n          entry.closed = true;\n          return __WASI_ESUCCESS;\n        }\n        if (wantWr && !entry.halfClosedWr) {\n          entry.halfClosedWr = true;\n        }\n        if (wantRd && !entry.eof) {\n          if (entry.reader) {\n            try {\n              await entry.reader.cancel();\n            } catch {\n            }\n          }\n          entry.eof = true;\n        }\n      } catch (e) {\n        return __WASI_EIO;\n      }\n      return __WASI_ESUCCESS;\n    },\n    async sock_accept(fd, flags, fdOutPtr) {\n      const entry = fdTable.get(fd);\n      if (!entry) return __WASI_EBADF;\n      if (entry.kind !== \"listener\") return __WASI_ENOTSOCK;\n      const kernel = __wasiKernelOrNull(\"connections cannot be accepted\");\n      if (!kernel) return __WASI_ENOSYS;\n      const nonblock = ((entry.fdflags | flags) & __WASI_FDFLAGS_NONBLOCK) !== 0;\n      try {\n        let accepted;\n        if (nonblock) {\n          accepted = kernel.acceptNow(entry.port);\n          if (!accepted) return __WASI_EAGAIN;\n        } else {\n          accepted = await kernel.accept(entry.port);\n        }\n        const socket = kernel.streamFor(accepted.id);\n        writeU32LE(fdOutPtr, __wasiAdoptSocket(socket, flags & __WASI_FDFLAGS_NONBLOCK));\n        return __WASI_ESUCCESS;\n      } catch (e) {\n        globalThis.__nimbusWasiLastSocketError = e && e.message ? e.message : String(e);\n        return __WASI_ENOTCONN;\n      }\n    }\n  };\n  installAuthorityFilesystem(imports, {\n    fs: () => __wasiSup ? supervisorFilesystem(__wasiSup, opts.parking === \"none\" ? __wasiSup.synchronous : void 0) : null,\n    memory: opts.getMemory,\n    fds: fdTable,\n    allocateFd: __wasiAllocateFd,\n    abi: opts.abi,\n    synchronous: opts.parking === \"none\",\n    residentBytes: __wasiFS.residentFileCap\n  });\n  const __rawSockRecv = imports.sock_recv;\n  const __rawSockSend = imports.sock_send;\n  const parkable = [\n    \"sock_send\",\n    \"sock_recv\",\n    \"sock_shutdown\",\n    \"sock_accept\",\n    \"poll_oneoff\",\n    \"fd_read\",\n    \"fd_write\",\n    \"fd_pread\",\n    \"fd_pwrite\",\n    \"path_filestat_get\",\n    \"path_open\",\n    \"fd_close\",\n    \"fd_renumber\",\n    \"fd_seek\",\n    \"fd_tell\",\n    \"fd_filestat_get\",\n    \"fd_fdstat_set_flags\",\n    \"fd_fdstat_set_rights\",\n    \"fd_filestat_set_size\",\n    \"fd_sync\",\n    \"fd_datasync\",\n    \"fd_allocate\",\n    \"fd_advise\",\n    \"fd_readdir\",\n    \"path_create_directory\",\n    \"path_remove_directory\",\n    \"path_unlink_file\",\n    \"path_rename\",\n    \"path_symlink\",\n    \"path_readlink\",\n    \"path_link\",\n    \"fd_filestat_set_times\",\n    \"path_filestat_set_times\"\n  ];\n  for (const name of parkable) {\n    if (typeof imports[name] === \"function\") imports[name] = withParkDeadline(imports[name]);\n  }\n  if (opts.parking === \"none\") {\n    if (opts.threads) {\n      throw new Error(\n        \"wasi: a threads build cannot run with parking:none; its threads block on a software futex, which requires the guest to be entered under WebAssembly.promising\"\n      );\n    }\n    for (const name of parkable) {\n      const fn = imports[name];\n      if (typeof fn !== \"function\") continue;\n      imports[name] = function noPark(...args) {\n        const r = fn.apply(this, args);\n        if (r && typeof r.then === \"function\") {\n          throw new Error(\n            \"wasi: \" + name + \" parked in a non-suspending instance; a guest that cannot be entered under WebAssembly.promising must not be given a filesystem or socket that blocks\"\n          );\n        }\n        return r;\n      };\n    }\n  } else if (typeof WebAssembly !== \"undefined\" && typeof WebAssembly.Suspending === \"function\") {\n    imports.sock_send = new WebAssembly.Suspending(imports.sock_send);\n    imports.sock_recv = new WebAssembly.Suspending(imports.sock_recv);\n    imports.sock_shutdown = new WebAssembly.Suspending(imports.sock_shutdown);\n    imports.sock_accept = new WebAssembly.Suspending(imports.sock_accept);\n    imports.poll_oneoff = new WebAssembly.Suspending(imports.poll_oneoff);\n    imports.fd_read = new WebAssembly.Suspending(imports.fd_read);\n    imports.fd_write = new WebAssembly.Suspending(imports.fd_write);\n    imports.fd_pread = new WebAssembly.Suspending(imports.fd_pread);\n    imports.fd_pwrite = new WebAssembly.Suspending(imports.fd_pwrite);\n    imports.path_filestat_get = new WebAssembly.Suspending(imports.path_filestat_get);\n    imports.path_open = new WebAssembly.Suspending(imports.path_open);\n    imports.fd_close = new WebAssembly.Suspending(imports.fd_close);\n    imports.fd_renumber = new WebAssembly.Suspending(imports.fd_renumber);\n    imports.fd_seek = new WebAssembly.Suspending(imports.fd_seek);\n    imports.fd_tell = new WebAssembly.Suspending(imports.fd_tell);\n    imports.fd_filestat_get = new WebAssembly.Suspending(imports.fd_filestat_get);\n    imports.fd_fdstat_set_flags = new WebAssembly.Suspending(imports.fd_fdstat_set_flags);\n    imports.fd_fdstat_set_rights = new WebAssembly.Suspending(imports.fd_fdstat_set_rights);\n    imports.fd_filestat_set_size = new WebAssembly.Suspending(imports.fd_filestat_set_size);\n    imports.fd_sync = new WebAssembly.Suspending(imports.fd_sync);\n    imports.fd_datasync = new WebAssembly.Suspending(imports.fd_datasync);\n    imports.fd_allocate = new WebAssembly.Suspending(imports.fd_allocate);\n    imports.fd_advise = new WebAssembly.Suspending(imports.fd_advise);\n    imports.fd_readdir = new WebAssembly.Suspending(imports.fd_readdir);\n    imports.path_create_directory = new WebAssembly.Suspending(imports.path_create_directory);\n    imports.path_remove_directory = new WebAssembly.Suspending(imports.path_remove_directory);\n    imports.path_unlink_file = new WebAssembly.Suspending(imports.path_unlink_file);\n    imports.path_rename = new WebAssembly.Suspending(imports.path_rename);\n    imports.path_symlink = new WebAssembly.Suspending(imports.path_symlink);\n    imports.path_readlink = new WebAssembly.Suspending(imports.path_readlink);\n    imports.path_link = new WebAssembly.Suspending(imports.path_link);\n    imports.fd_filestat_set_times = new WebAssembly.Suspending(imports.fd_filestat_set_times);\n    imports.path_filestat_set_times = new WebAssembly.Suspending(imports.path_filestat_set_times);\n    if (opts.threads) imports.sched_yield = new WebAssembly.Suspending(imports.sched_yield);\n  }\n  return {\n    wasiImport: imports,\n    getStdout: () => stdoutBuf,\n    getStderr: () => stderrBuf\n  };\n}\nfunction __wasiRunStart(instance, ctx) {\n  try {\n    const start = instance.exports._start;\n    if (typeof start !== \"function\") {\n      return { exitCode: 1, error: \"_start is not a function (got \" + typeof start + \")\" };\n    }\n    start();\n    return { exitCode: 0 };\n  } catch (e) {\n    if (e && e.constructor && e.constructor.name === \"__WasiExit\") {\n      return { exitCode: e.code };\n    }\n    return { exitCode: 1, error: e && e.message ? e.message : String(e) };\n  }\n}\nasync function __wasiRunStartAsync(instance, ctx) {\n  try {\n    const start = instance.exports._start;\n    if (typeof start !== \"function\") {\n      return { exitCode: 1, error: \"_start is not a function (got \" + typeof start + \")\" };\n    }\n    if (typeof WebAssembly !== \"undefined\" && typeof WebAssembly.promising === \"function\") {\n      const promisingStart = WebAssembly.promising(start);\n      await promisingStart();\n    } else {\n      start();\n    }\n    return { exitCode: 0 };\n  } catch (e) {\n    if (e && e.constructor && e.constructor.name === \"__WasiExit\") {\n      return { exitCode: e.code };\n    }\n    return { exitCode: 1, error: e && e.message ? e.message : String(e) };\n  }\n}\nglobalThis.__wasiAdoptSupervisor = __wasiAdoptSupervisor;";
