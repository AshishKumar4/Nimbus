// Nimbus build stub: @opentui/core's per-platform native packages carry the
// Zig FFI dylib; Nimbus renders through the wasm32-wasi reactor instead
// (bundle-patches.ts reroutes resolution to the parked backend).
export default {}
