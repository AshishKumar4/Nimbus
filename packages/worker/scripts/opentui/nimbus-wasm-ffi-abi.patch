diff --git a/packages/core/src/zig/event-bus.zig b/packages/core/src/zig/event-bus.zig
index 2dd0586a..3cd7b5ee 100644
--- a/packages/core/src/zig/event-bus.zig
+++ b/packages/core/src/zig/event-bus.zig
@@ -1,7 +1,21 @@
 const std = @import("std");
+const builtin = @import("builtin");
 
 pub const EventCallback = *const fn (namePtr: [*]const u8, nameLen: usize, dataPtr: [*]const u8, dataLen: usize) callconv(.c) void;
 
+/// On wasm the callback registered via `createEventSink` is an opaque
+/// non-null host token, not a callable pointer (see logger.zig); emission
+/// routes through this import with the token. Never referenced on native.
+extern "opentui" fn eventSinkCallback(token: usize, namePtr: [*]const u8, nameLen: usize, dataPtr: [*]const u8, dataLen: usize) void;
+
+inline fn invokeEventCallback(callback: EventCallback, name: []const u8, data: []const u8) void {
+    if (comptime builtin.target.cpu.arch.isWasm()) {
+        eventSinkCallback(@intFromPtr(callback), name.ptr, name.len, data.ptr, data.len);
+    } else {
+        callback(name.ptr, name.len, data.ptr, data.len);
+    }
+}
+
 pub const EventSink = struct {
     callback: ?EventCallback,
 };
@@ -19,6 +33,6 @@ pub fn destroyEventSink(allocator: std.mem.Allocator, sink: *EventSink) void {
 
 pub fn emit(sink: ?*EventSink, name: []const u8, data: []const u8) void {
     if (sink) |event_sink| {
-        if (event_sink.callback) |callback| callback(name.ptr, name.len, data.ptr, data.len);
+        if (event_sink.callback) |callback| invokeEventCallback(callback, name, data);
     }
 }
diff --git a/packages/core/src/zig/lib.zig b/packages/core/src/zig/lib.zig
index 5f33e48b..146aabb5 100644
--- a/packages/core/src/zig/lib.zig
+++ b/packages/core/src/zig/lib.zig
@@ -68,10 +68,42 @@ var gpa: std.heap.GeneralPurposeAllocator(.{
     .enable_memory_limit = build_options.gpa_safe_stats,
     .safety = build_options.gpa_safe_stats,
 }) = .{};
-const globalAllocator = gpa.allocator();
+/// On wasm the GPA's backing page allocator can never unmap: every freed
+/// large allocation and emptied bucket page is linear memory lost forever,
+/// so alloc/free churn grows the module until the host's memory cap
+/// (measured: a ~180-byte styled-text set grew linear memory ~50 MB). Use
+/// std.heap.wasm_allocator — its size-class freelists reuse freed memory —
+/// as the global backing allocator on wasm; native keeps the GPA unchanged.
+const globalAllocator = if (@import("builtin").target.cpu.arch.isWasm())
+    std.heap.wasm_allocator
+else
+    gpa.allocator();
 var arena = std.heap.ArenaAllocator.init(globalAllocator);
 const globalArena = arena.allocator();
 
+/// wasm copy-in/copy-back arena protocol: the host has no FFI views into
+/// linear memory, so it allocates scratch regions through these exports,
+/// writes arguments in, calls the FFI, reads results back, and frees.
+/// 16-byte alignment covers every extern struct and scalar layout in the
+/// FFI surface. wasm-only: the native dylib export surface is unchanged.
+fn nimbusAlloc(size: usize) callconv(.c) ?[*]u8 {
+    const mem = globalAllocator.alignedAlloc(u8, .@"16", size) catch return null;
+    return mem.ptr;
+}
+
+fn nimbusFree(ptr: ?[*]u8, size: usize) callconv(.c) void {
+    const base = ptr orelse return;
+    const slice: []align(16) u8 = @alignCast(base[0..size]);
+    globalAllocator.free(slice);
+}
+
+comptime {
+    if (@import("builtin").target.cpu.arch.isWasm()) {
+        @export(&nimbusAlloc, .{ .name = "nimbus_alloc" });
+        @export(&nimbusFree, .{ .name = "nimbus_free" });
+    }
+}
+
 pub const ExternalBuildOptions = extern struct {
     gpa_safe_stats: bool,
     gpa_memory_limit_tracking: bool,
@@ -600,10 +632,12 @@ pub const ExternalCapabilities = extern struct {
     explicit_cursor_positioning: bool,
     remote: bool,
     multiplexer: u8,
+    // Sized u64 to match the host FFI packer's 64-bit usize assumption on
+    // wasm (see text-buffer.zig StyledChunk); native keeps usize.
     term_name_ptr: [*]const u8,
-    term_name_len: usize,
+    term_name_len: if (@import("builtin").target.cpu.arch.isWasm()) u64 else usize,
     term_version_ptr: [*]const u8,
-    term_version_len: usize,
+    term_version_len: if (@import("builtin").target.cpu.arch.isWasm()) u64 else usize,
     term_from_xtversion: bool,
 };
 
diff --git a/packages/core/src/zig/logger.zig b/packages/core/src/zig/logger.zig
index b9b72c07..fae1f755 100644
--- a/packages/core/src/zig/logger.zig
+++ b/packages/core/src/zig/logger.zig
@@ -1,4 +1,5 @@
 const std = @import("std");
+const builtin = @import("builtin");
 
 pub const LogLevel = enum(u8) {
     err = 0,
@@ -7,22 +8,39 @@ pub const LogLevel = enum(u8) {
     debug = 3,
 };
 
-var global_log_callback: ?*const fn (level: u8, msgPtr: [*]const u8, msgLen: usize) callconv(.c) void = null;
+pub const LogCallback = *const fn (level: u8, msgPtr: [*]const u8, msgLen: usize) callconv(.c) void;
 
-pub fn setLogCallback(callback: ?*const fn (level: u8, msgPtr: [*]const u8, msgLen: usize) callconv(.c) void) void {
+var global_log_callback: ?LogCallback = null;
+
+pub fn setLogCallback(callback: ?LogCallback) void {
     global_log_callback = callback;
 }
 
+/// On wasm the host cannot place its functions in the module's indirect
+/// function table, so the "pointer" registered via `setLogCallback` is an
+/// opaque non-null token minted by the host. Invocation routes through this
+/// import, handing the token back; native builds call the fn pointer
+/// directly and never reference (or import) this symbol.
+extern "opentui" fn logCallback(token: usize, level: u8, msgPtr: [*]const u8, msgLen: usize) void;
+
+inline fn invokeLogCallback(callback: LogCallback, level: u8, msgPtr: [*]const u8, msgLen: usize) void {
+    if (comptime builtin.target.cpu.arch.isWasm()) {
+        logCallback(@intFromPtr(callback), level, msgPtr, msgLen);
+    } else {
+        callback(level, msgPtr, msgLen);
+    }
+}
+
 // Helper function to log messages - can be used directly throughout the codebase
 pub fn logMessage(level: LogLevel, comptime format: []const u8, args: anytype) void {
     if (global_log_callback) |callback| {
         var buf: [4096]u8 = undefined;
         const msg = std.fmt.bufPrint(&buf, format, args) catch {
             const fallback = "Log formatting failed";
-            callback(@intFromEnum(LogLevel.err), fallback.ptr, fallback.len);
+            invokeLogCallback(callback, @intFromEnum(LogLevel.err), fallback.ptr, fallback.len);
             return;
         };
-        callback(@intFromEnum(level), msg.ptr, msg.len);
+        invokeLogCallback(callback, @intFromEnum(level), msg.ptr, msg.len);
     }
 }
 
diff --git a/packages/core/src/zig/native-span-feed.zig b/packages/core/src/zig/native-span-feed.zig
index 9ffd9212..2ba38928 100644
--- a/packages/core/src/zig/native-span-feed.zig
+++ b/packages/core/src/zig/native-span-feed.zig
@@ -1,7 +1,22 @@
 const std = @import("std");
+const builtin = @import("builtin");
 
 pub const CallbackFn = fn (stream_ptr: usize, event_id: u32, arg0: usize, arg1: u64) callconv(.c) void;
 
+/// On wasm the callback registered via `streamSetCallback` is an opaque
+/// non-null host token, not a callable pointer (see logger.zig); span-feed
+/// events route through this import with the token. Never referenced on
+/// native.
+extern "opentui" fn streamCallback(token: usize, stream_ptr: usize, event_id: u32, arg0: usize, arg1: u64) void;
+
+inline fn invokeStreamCallback(callback: *const CallbackFn, stream_ptr: usize, event_id: u32, arg0: usize, arg1: u64) void {
+    if (comptime builtin.target.cpu.arch.isWasm()) {
+        streamCallback(@intFromPtr(callback), stream_ptr, event_id, arg0, arg1);
+    } else {
+        callback(stream_ptr, event_id, arg0, arg1);
+    }
+}
+
 pub const GrowthPolicy = enum(u8) {
     grow = 0,
     block = 1,
@@ -546,25 +561,25 @@ pub const Stream = struct {
 
     fn emitChunkAdded(self: *Stream, chunk: Chunk) void {
         if (self.callback) |cb| {
-            cb(@intFromPtr(self), Event.ChunkAdded, @intFromPtr(chunk.ptr), chunk.len);
+            invokeStreamCallback(cb, @intFromPtr(self), Event.ChunkAdded, @intFromPtr(chunk.ptr), chunk.len);
         }
     }
 
     fn emitDataAvailable(self: *Stream, count: u32) void {
         if (self.callback) |cb| {
-            cb(@intFromPtr(self), Event.DataAvailable, count, 0);
+            invokeStreamCallback(cb, @intFromPtr(self), Event.DataAvailable, count, 0);
         }
     }
 
     fn emitStateBuffer(self: *Stream) void {
         if (self.callback) |cb| {
-            cb(@intFromPtr(self), Event.StateBuffer, @intFromPtr(self.state_buffer.ptr), self.state_capacity);
+            invokeStreamCallback(cb, @intFromPtr(self), Event.StateBuffer, @intFromPtr(self.state_buffer.ptr), self.state_capacity);
         }
     }
 
     fn emitClosed(self: *Stream) void {
         if (self.callback) |cb| {
-            cb(@intFromPtr(self), Event.Closed, 0, 0);
+            invokeStreamCallback(cb, @intFromPtr(self), Event.Closed, 0, 0);
         }
     }
 };
diff --git a/packages/core/src/zig/text-buffer.zig b/packages/core/src/zig/text-buffer.zig
index 870238ae..8041fe34 100644
--- a/packages/core/src/zig/text-buffer.zig
+++ b/packages/core/src/zig/text-buffer.zig
@@ -40,15 +40,21 @@ pub const TextBuffer = UnifiedTextBuffer;
 /// The color pointers point to 4 consecutive u16 values in the packed RGBA
 /// format defined by ansi.zig. Use utils.ptrToRGBA to read them.
 pub const StyledChunk = extern struct {
+    /// The host FFI struct packer sizes these length fields as u64 (every
+    /// Bun-native target is 64-bit, so upstream's JS declares them "u64").
+    /// The wasm32 reactor must match that host ABI or every later field is
+    /// read shifted — link_len then lands on the packed link POINTER, and a
+    /// ~18 MB garbage "URL" gets copied into the link pool per styled-text
+    /// set. Native keeps usize (identical layout on 64-bit).
     text_ptr: [*]const u8,
-    text_len: usize,
+    text_len: if (@import("builtin").target.cpu.arch.isWasm()) u64 else usize,
     /// Optional foreground color as 4 packed u16 values (see ansi.RGBA).
     fg_ptr: ?[*]const u16,
     /// Optional background color as 4 packed u16 values (see ansi.RGBA).
     bg_ptr: ?[*]const u16,
     attributes: u32,
     link_ptr: ?[*]const u8 = null,
-    link_len: usize = 0,
+    link_len: if (@import("builtin").target.cpu.arch.isWasm()) u64 else usize = 0,
 };
 
 pub const UnifiedTextBuffer = struct {
@@ -1088,7 +1094,7 @@ pub const UnifiedTextBuffer = struct {
         // Calculate total text length
         var total_len: usize = 0;
         for (chunks) |chunk| {
-            total_len += chunk.text_len;
+            total_len += @as(usize, @intCast(chunk.text_len));
         }
 
         if (total_len == 0) {
@@ -1117,10 +1123,11 @@ pub const UnifiedTextBuffer = struct {
 
         var offset: usize = 0;
         for (chunks) |chunk| {
-            if (chunk.text_len > 0) {
-                const chunk_text = chunk.text_ptr[0..chunk.text_len];
-                @memcpy(full_text[offset .. offset + chunk.text_len], chunk_text);
-                offset += chunk.text_len;
+            const text_len: usize = @intCast(chunk.text_len);
+            if (text_len > 0) {
+                const chunk_text = chunk.text_ptr[0..text_len];
+                @memcpy(full_text[offset .. offset + text_len], chunk_text);
+                offset += text_len;
             }
         }
 
@@ -1142,7 +1149,7 @@ pub const UnifiedTextBuffer = struct {
 
             var char_pos: u32 = 0;
             for (chunks, 0..) |chunk, i| {
-                const chunk_text = chunk.text_ptr[0..chunk.text_len];
+                const chunk_text = chunk.text_ptr[0..@as(usize, @intCast(chunk.text_len))];
                 const chunk_len = self.measureText(chunk_text);
 
                 if (chunk_len > 0) {
@@ -1151,9 +1158,10 @@ pub const UnifiedTextBuffer = struct {
 
                     var attributes = chunk.attributes;
                     if (chunk.link_ptr) |link_ptr| {
-                        if (chunk.link_len > 0) {
+                        const link_len: usize = @intCast(chunk.link_len);
+                        if (link_len > 0) {
                             const tracker = self.getLinkTracker();
-                            const url = link_ptr[0..chunk.link_len];
+                            const url = link_ptr[0..link_len];
                             const link_id = tracker.pool.alloc(url) catch 0;
                             if (link_id != 0) {
                                 const maybe_seen = seen_link_ids.getOrPut(self.global_allocator, link_id) catch null;
