# Build tree + intermediates (regenerated by build-bash.sh). The final
# proven artifact bash.async.wasm IS committed — it is the milestone
# deliverable and the deploy stages it to R2 from here; rebuilding needs
# wasi-sdk-25 + binaryen (heavy). coreutils/*.wasm are committed likewise.
bash-5.2.37/
bash-5.2.37.tar.gz
bash.fpc.wasm
*.o

# BusyBox build tree + intermediates (regenerated by coreutils/build-busybox.sh);
# the proven busybox.wasm + busybox.applets artifacts ARE committed, like
# bash.async.wasm above.
coreutils/busybox-1.37.0/
coreutils/busybox-1.37.0.tar.bz2
