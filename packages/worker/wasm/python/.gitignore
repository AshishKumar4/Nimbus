# Build tree + intermediates, regenerated by build-python.sh. Rebuilding needs
# wasi-sdk-25 and ~25 minutes; the proven artifacts below ARE committed, exactly
# as bash.async.wasm is in ../bash.
build/
