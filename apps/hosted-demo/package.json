{
  "name": "@nimbus-sh/hosted-demo",
  "version": "0.1.0",
  "description": "Nimbus live demo — the canonical SDK embedder. This is what runs at nimbus-os.dev.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "predev": "bun run --cwd ../../packages/worker bundle && bun run build:assets",
    "predeploy": "bun run --cwd ../../packages/worker bundle && bun run build:assets",
    "dev": "wrangler dev --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "deploy:production": "bun run --cwd ../../packages/worker bundle && bun run build:assets && wrangler deploy -e production"
  },
  "dependencies": {
    "@nimbus-sh/sdk": "workspace:^",
    "@nimbus-sh/worker": "workspace:^"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
