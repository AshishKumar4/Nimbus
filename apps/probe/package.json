{
  "name": "@nimbus-sh/probe",
  "version": "0.1.0",
  "description": "Authenticated probe target for the behavioral suite — a minimal JWT-enforced Nimbus embedder.",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "bun run --cwd ../../packages/worker bundle",
    "predeploy": "bun ../../scripts/dist-integrity.mjs",
    "dev": "wrangler dev --ip 0.0.0.0 --port 8792",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@nimbus-sh/sdk": "workspace:^",
    "@nimbus-sh/worker": "workspace:^"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
